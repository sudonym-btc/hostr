name: escrow
description: A Dart service that responds to Nostr messages and exposes a CLI.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  dart_nostr: ^8.0.3
  nostr: 
  args: ^2.0.0
  get_it: ^8.0.3
  injectable: ^2.1.1
  shelf: ^1.2.0
  shelf_router: ^1.0.0
  http: ^1.2.0 

dev_dependencies:
  test: ^1.16.0