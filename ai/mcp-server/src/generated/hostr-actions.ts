// Generated by hostr_cli/bin/generate_mcp_types.dart.
// Source of truth: hostr_cli/lib/src/actions/hostr_actions.dart.
import { z } from "zod";

export interface HostrSessionStatusInput {
  /** Include non-secret storage/session diagnostics useful for debugging. */
  includeStorageDetails?: boolean;
}

export interface HostrSessionConnectInput {
  /** False returns an active nostrconnect URI/QR for display. True waits for the shown request to connect; call it immediately after displaying the QR so the original Hostr action can continue. */
  wait?: boolean;
  /** How long to wait for approval when wait is true. Defaults to 180, capped at 600. */
  timeoutSeconds?: number;
  /** Force a fresh nostrconnect request instead of reusing the pending one. */
  regenerate?: boolean;
}

export interface HostrListingsSearchInput {
  /** Human-readable destination/place to search for Hostr stays, lodging, accommodation, rentals, rooms, apartments, hotels, villas, resorts, or places to stay, such as "San Salvador", "El Salvador", or "Lisbon". */
  location?: string;
  /** Client-side text filter over listing title and description. */
  query?: string;
  /** Hostr listing type, for example room, house, apartment, cabin, or villa. */
  type?: string;
  /** Minimum guest capacity. */
  guests?: number;
  /** Required listing specifications/features, such as wifi or kitchen. */
  features?: string[];
  /** Maximum number of listings to return. Defaults to 10, capped at 50. */
  limit?: number;
}

export interface HostrListingsListInput {
  mine?: boolean;
  author?: string;
  limit?: number;
}

export interface HostrAmountInput {
  /** Decimal payment amount, represented as a string to avoid precision loss. */
  value: string;
  /** Currency or denomination, such as USD or BTC. */
  currency: string;
  /** Optional display/base unit such as sats. */
  unit?: string;
  /** Optional decimal precision for raw-denomination amounts. */
  decimals?: number;
}

export interface HostrListingImageInput {
  /** Required image URL. For user-uploaded files, use the upload.url returned by POST /mcp/uploads/images on the same Hostr MCP origin. The upload endpoint does not require or use Authorization. */
  url: string;
  /** Optional original filename used for MIME detection and upload diagnostics. */
  filename?: string;
  /** Alt text for the image. */
  alt?: string;
  /** Optional MIME type override. */
  mime?: string;
}

export interface HostrListingPriceInput {
  amount: HostrAmountInput;
  /** daily, weekly, monthly, yearly, or fixed. Defaults to daily. */
  frequency?: string;
}

export interface HostrListingsCreateInput {
  title: string;
  description: string;
  /** Precise address used for H3 tag generation. */
  address: string;
  images: HostrListingImageInput[];
  prices: HostrListingPriceInput[];
  type?: string;
  specifications?: Record<string, unknown>;
  guests?: number;
  beds?: number;
  bedrooms?: number;
  bathrooms?: number;
  active?: boolean;
  negotiable?: boolean;
  instantBook?: boolean;
  minStay?: number;
  checkIn?: string;
  checkOut?: string;
  quantity?: number;
  securityDeposit?: HostrAmountInput;
  minPaymentAmount?: HostrAmountInput;
  h3Tags?: string[];
  h3FinestResolution?: number;
  h3MaxTags?: number;
  /** True uploads local images to Blossom and previews only. Set false to publish after explicit user approval. */
  dryRun?: boolean;
}

export interface HostrListingsEditInput {
  /** Listing naddr/a-tag anchor. */
  anchor: string;
  /** Listing fields to change. */
  patch?: Partial<HostrListingsCreateInput>;
  /** True previews only. Set false to publish after explicit approval. */
  dryRun?: boolean;
}

export interface HostrListingsAvailabilityInput {
  /** Listing anchors to check. */
  anchors?: string[];
  /** Single listing anchor alternative. */
  anchor?: string;
  /** Requested start calendar date encoded as YYYY-MM-DDT00:00:00Z. Do not timezone-convert date-only reservation inputs. */
  start: string;
  /** Requested end calendar date encoded as YYYY-MM-DDT00:00:00Z. Do not timezone-convert date-only reservation inputs. */
  end: string;
}

export interface HostrListingsAnchorsInput {
  anchors?: string[];
  anchor?: string;
  limit?: number;
}

export interface HostrReservationBookAndPayInput {
  /** Listing naddr/a-tag anchor to instant-book. */
  listingAnchor: string;
  /** Reservation start calendar date encoded as YYYY-MM-DDT00:00:00Z. Do not timezone-convert date-only reservation inputs. */
  start: string;
  /** Reservation end calendar date encoded as YYYY-MM-DDT00:00:00Z. Do not timezone-convert date-only reservation inputs. */
  end: string;
  /** Optional reservation amount override. Must be at or above the listing price. */
  amount?: HostrAmountInput;
  /** Optional escrow service id/pubkey/contract address. */
  escrowServiceId?: string;
  /** Seconds to wait for the global reservation stream to emit the committed reservation. */
  proofTimeoutSeconds?: number;
}

export interface HostrAmountInput {
  /** Decimal payment amount, represented as a string to avoid precision loss. */
  value: string;
  /** Currency or denomination, such as USD or BTC. */
  currency: string;
  /** Optional display/base unit such as sats. */
  unit?: string;
  /** Optional decimal precision for raw-denomination amounts. */
  decimals?: number;
}

export interface HostrReservationsOfferInput {
  /** Listing naddr/a-tag anchor for a first offer. */
  listingAnchor?: string;
  /** Single listing anchor alternative to listingAnchor. */
  anchor?: string;
  /** Existing reservation trade id for a follow-up offer. */
  tradeId?: string;
  /** Reservation start calendar date for a first offer, encoded as YYYY-MM-DDT00:00:00Z. Do not timezone-convert date-only reservation inputs. */
  start?: string;
  /** Reservation end calendar date for a first offer, encoded as YYYY-MM-DDT00:00:00Z. Do not timezone-convert date-only reservation inputs. */
  end?: string;
  /** Optional reservation amount override. Omit to use listing price rules. */
  amount?: HostrAmountInput;
  /** True previews only. Set false to send the gift-wrapped offer after user approval. */
  dryRun?: boolean;
  timeoutSeconds?: number;
}

export interface HostrReservationTradeInput {
  tradeId: string;
  amount?: HostrAmountInput;
  reason?: string;
  dryRun?: boolean;
  timeoutSeconds?: number;
}

export interface HostrReservationPayInput {
  /** Reservation trade id from negotiation updates. */
  tradeId: string;
  /** Optional escrow service id/pubkey/contract address. */
  escrowServiceId?: string;
  dryRun?: boolean;
  timeoutSeconds?: number;
}

export interface HostrReservationCommitInput {
  /** Boltz swap id returned by hostr_reservations_pay. */
  swapId: string;
  dryRun?: boolean;
  timeoutSeconds?: number;
}

export interface HostrUpdatesInput {
  /** Maximum inbox events to fetch. */
  limit?: number;
  /** Seconds to wait for relay history before returning partial results. */
  timeoutSeconds?: number;
}

export interface HostrReplyInput {
  content: string;
  recipientPubkeys?: string[];
  recipientPubkey?: string;
  conversation?: string;
  tradeId?: string;
  dryRun?: boolean;
}

export interface HostrThreadViewInput {
  threadAnchor?: string;
  anchor?: string;
  conversation?: string;
  tradeId?: string;
  recipientPubkeys?: string[];
  recipientPubkey?: string;
  limit?: number;
  timeoutSeconds?: number;
}

export interface HostrThreadMessageInput {
  content: string;
  threadAnchor?: string;
  anchor?: string;
  conversation?: string;
  tradeId?: string;
  recipientRole?: "host" | "seller" | "guest" | "buyer" | "escrow";
  role?: string;
  recipientPubkeys?: string[];
  recipientPubkey?: string;
  dryRun?: boolean;
  timeoutSeconds?: number;
}

export interface HostrEscrowInvolveInput {
  tradeId: string;
  content?: string;
  message?: string;
  dryRun?: boolean;
  timeoutSeconds?: number;
}

export interface HostrEmptyInput {}

export interface HostrProfileEditInput {
  name?: string;
  about?: string;
  image?: string;
  picture?: string;
  lud16?: string;
  nip05?: string;
  dryRun?: boolean;
}

export interface HostrReservationCollectionInput {
  limit?: number;
  tradeId?: string;
  waitSeconds?: number;
}

export interface HostrEscrowMethodsInput {
  /** Seller/host pubkey to inspect escrow compatibility for. */
  user: string;
  /** Buyer pubkey. Defaults to the authenticated token pubkey. */
  buyer?: string;
}

export interface HostrEscrowServiceListInput {
  /** Maximum number of escrow service events to return. Defaults to 25, capped at 100. */
  limit?: number;
}

export interface HostrEscrowServiceGetInput {
  /** Escrow service event id to inspect. */
  serviceId: string;
}

export interface HostrTokenFeeHintsInput {
  /** Flat base fee in token smallest units. */
  baseFee?: number;
  /** Maximum fee cap in token smallest units. Zero means no cap. */
  maxFee?: number;
  /** Minimum fee floor in token smallest units. Zero means no floor. */
  minFee?: number;
}

export interface HostrEscrowServiceUpdateInput {
  /** Optional escrow service event id to edit. Omit to edit the daemon bootstrap service. */
  serviceId?: string;
  /** Proportional escrow fee as a percent, e.g. 1.5 for 1.5%. */
  feePercent?: number;
  /** Maximum supported escrow duration in seconds. */
  maxDurationSeconds?: number;
  /** Full replacement map keyed by token address, or "native". */
  tokenFeeHints?: Record<string, HostrTokenFeeHintsInput>;
  /** Clear all per-token fee hints. Ignored if tokenFeeHints is provided. */
  clearTokenFeeHints?: boolean;
  /** Defaults to true. Set false only after explicit approval. */
  dryRun?: boolean;
}

export interface HostrEscrowServiceDeleteInput {
  /** Escrow service event id to delete. */
  serviceId: string;
  /** Optional deletion reason for the NIP-09 event. */
  reason?: string;
  /** Defaults to true. Set false only after explicit approval. */
  dryRun?: boolean;
}

export interface HostrEscrowTradesListInput {
  /** Maximum number of escrow trades to return. Defaults to 25, capped at 100. */
  limit?: number;
}

export interface HostrEscrowTradeViewInput {
  /** Hostr reservation trade id to inspect. */
  tradeId: string;
}

export interface HostrEscrowTradeAuditInput {
  /** Hostr reservation trade id to audit. */
  tradeId: string;
}

export interface HostrEscrowArbitrateInput {
  /** Hostr reservation trade id to arbitrate. */
  tradeId: string;
  /** Fraction of the escrowed payment to forward to the seller, from 0 to 1. */
  paymentForward: number;
  /** Fraction of the escrow bond to forward according to contract settlement rules, from 0 to 1. */
  bondForward: number;
  /** Optional human-readable arbitration reason. */
  reason?: string;
  /** Defaults to true. Set false only after explicit approval. */
  dryRun?: boolean;
}

export interface HostrEscrowBadgeDefinitionsListInput {
  limit?: number;
}

export interface HostrEscrowBadgeDefinitionEditInput {
  /** Badge definition d-tag identifier. */
  identifier: string;
  name: string;
  description?: string;
  image?: string;
  dryRun?: boolean;
}

export interface HostrEscrowBadgeDefinitionDeleteInput {
  anchor: string;
  reason?: string;
  dryRun?: boolean;
}

export interface HostrEscrowBadgeAwardsListInput {
  definitionAnchor?: string;
  limit?: number;
}

export interface HostrEscrowBadgeAwardInput {
  definitionAnchor: string;
  recipientPubkey: string;
  listingAnchor?: string;
  dryRun?: boolean;
}

export interface HostrEscrowBadgeRevokeInput {
  awardId: string;
  reason?: string;
  dryRun?: boolean;
}

export interface HostrSwapsWatchInput {
  swapId: string;
  tradeId?: string;
  reservationWaitSeconds?: number;
  dryRun?: boolean;
}

export interface HostrSwapsRecoverAllInput {
  background?: boolean;
  dryRun?: boolean;
}

export interface HostrSwapsListInput {
  namespace?: "all" | "swap_in" | "swap_out";
}

export const HostrSessionStatusInputSchema = z.object({
  "includeStorageDetails": z.boolean().describe("Include non-secret storage/session diagnostics useful for debugging.").default(false).optional()
}).strict();

export const HostrSessionConnectInputSchema = z.object({
  "wait": z.boolean().describe("False returns an active nostrconnect URI/QR for display. True waits for the already-shown request to connect; call it immediately after displaying the QR so the original Hostr action can continue.").default(false).optional(),
  "timeoutSeconds": z.number().int().min(1).max(600).describe("How long to wait for approval when wait is true.").default(180).optional(),
  "regenerate": z.boolean().describe("Force a fresh nostrconnect request instead of reusing the pending one.").default(false).optional()
}).strict();

export const HostrListingsSearchInputSchema = z.object({
  "location": z.string().describe("Human-readable destination/place to search for Hostr stays, lodging, accommodation, rentals, rooms, apartments, hotels, villas, resorts, or places to stay, such as \"San Salvador\", \"El Salvador\", or \"Lisbon\".").optional(),
  "query": z.string().describe("Client-side text filter over listing title and description.").optional(),
  "type": z.string().describe("Hostr listing type, for example room, house, apartment, cabin, or villa.").optional(),
  "guests": z.number().int().min(1).describe("Minimum guest capacity.").optional(),
  "features": z.array(z.string()).describe("Required listing specifications/features, such as wifi or kitchen.").optional(),
  "limit": z.number().int().min(1).max(50).describe("Maximum number of listings to return.").default(10).optional()
}).strict();

export const HostrListingsListInputSchema = z.object({
  "mine": z.boolean().describe("Only list listings authored by the authenticated user.").default(false).optional(),
  "author": z.string().describe("Only list listings authored by this pubkey.").optional(),
  "limit": z.number().int().min(1).max(200).default(50).optional()
}).strict();

export const HostrListingsCreateInputSchema = z.object({
  "title": z.string().describe("Public listing title."),
  "description": z.string().describe("Public listing description."),
  "address": z.string().describe("Private-ish precise address used by Hostr to generate H3 tags."),
  "type": z.string().describe("Listing type, such as room, apartment, house, or villa.").optional(),
  "images": z.array(z.object({
  "url": z.string().describe("Required image URL. For user-uploaded files, this must be structuredContent.usage.listingImage.url returned by hostr_images_upload, or upload.url returned by POST /mcp/uploads/images on the same Hostr MCP origin; the upload tool and endpoint do not require or use Authorization. Public HTTP(S) source URLs are also accepted; Hostr downloads non-Blossom URLs and uploads them to Blossom before publishing."),
  "filename": z.string().describe("Optional original filename used for MIME detection and upload diagnostics.").optional(),
  "alt": z.string().describe("Image alt text.").optional(),
  "mime": z.string().describe("Optional MIME type, for example image/jpeg or image/png.").optional()
}).strict()).min(1).describe("Listing images. This MCP tool accepts image URLs only. Mandatory flow for user-uploaded files: first call hostr_images_upload with the original image sent as the MCP file-typed argument named file so the client bridge can rewrite or stream the bytes, then pass structuredContent.usage.listingImage.url here as images[].url. If the client cannot call hostr_images_upload but can make raw HTTP requests, POST each original file bytes payload to /mcp/uploads/images on this same MCP server origin using multipart/form-data field name file, then pass the returned upload.url here as images[].url. The upload tool and endpoint do not require or use Authorization; do not send OAuth, Nostr auth, or Hostr session credentials for image upload. Do not base64-encode uploaded images into this tool call. Do not start or serve a temporary localhost URL for the MCP server to fetch; localhost refers to the wrong machine/container. Do not resize, downscale, crop, recompress, transcode, or make thumbnails unless the user explicitly requests it. If neither upload route can be used, stop and ask for a public image URL or for the client to expose an upload capability. Never pass client-local or mounted file paths like /mnt/data, /mnt/shared, file://, or ChatGPT file mounts to images[].url."),
  "prices": z.array(z.object({
  "amount": z.object({
  "value": z.string(),
  "currency": z.string(),
  "unit": z.string().optional(),
  "decimals": z.number().int().min(0).optional()
}).strict(),
  "frequency": z.string().describe("daily, weekly, monthly, yearly, or fixed.").optional()
}).strict()).min(1).describe("Listing prices. All monetary fields must use the same currency."),
  "specifications": z.record(z.string(), z.unknown()).describe("Additional listing specifications, for example wifi, kitchen, or workspace.").optional(),
  "guests": z.number().int().min(1).optional(),
  "beds": z.number().int().min(0).optional(),
  "bedrooms": z.number().int().min(0).optional(),
  "bathrooms": z.number().int().min(0).optional(),
  "active": z.boolean().optional(),
  "negotiable": z.boolean().optional(),
  "instantBook": z.boolean().optional(),
  "minStay": z.number().int().min(1).optional(),
  "checkIn": z.string().optional(),
  "checkOut": z.string().optional(),
  "quantity": z.number().int().min(1).optional(),
  "securityDeposit": z.object({
  "value": z.string(),
  "currency": z.string(),
  "unit": z.string().optional(),
  "decimals": z.number().int().min(0).optional()
}).strict().optional(),
  "minPaymentAmount": z.object({
  "value": z.string(),
  "currency": z.string(),
  "unit": z.string().optional(),
  "decimals": z.number().int().min(0).optional()
}).strict().optional(),
  "h3Tags": z.array(z.string()).describe("Optional precomputed H3 tags. If omitted, address is geocoded.").optional(),
  "h3FinestResolution": z.number().int().min(0).optional(),
  "h3MaxTags": z.number().int().min(1).optional(),
  "dryRun": z.boolean().describe("True uploads local images to Blossom and previews the listing card only. Set false to publish after explicit approval.").default(true).optional()
}).strict();

export const HostrListingsEditInputSchema = z.object({
  "anchor": z.string().describe("Listing naddr/a-tag anchor."),
  "patch": z.record(z.string(), z.unknown()).describe("Listing fields to change. Supports title, description, address, type, images, prices, specifications, guests, beds, bedrooms, bathrooms, active, negotiable, instantBook, quantity, securityDeposit, and minPaymentAmount.").optional(),
  "dryRun": z.boolean().describe("True previews only. Set false to publish after explicit approval.").default(true).optional()
}).strict();

export const HostrListingsAvailabilityInputSchema = z.object({
  "anchors": z.array(z.string()).describe("Listing anchors to check.").optional(),
  "anchor": z.string().describe("Single listing anchor alternative to anchors.").optional(),
  "start": z.string().datetime().describe("Requested start calendar date encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time."),
  "end": z.string().datetime().describe("Requested end calendar date encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time.")
}).strict();

export const HostrListingsAnchorsInputSchema = z.object({
  "anchors": z.array(z.string()).describe("Listing anchors.").optional(),
  "anchor": z.string().describe("Single listing anchor.").optional(),
  "limit": z.number().int().min(1).max(200).default(50).optional()
}).strict();

export const HostrReservationBookAndPayInputSchema = z.object({
  "listingAnchor": z.string().describe("Listing naddr/a-tag anchor to instant-book."),
  "start": z.string().datetime().describe("Reservation start calendar date encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time."),
  "end": z.string().datetime().describe("Reservation end calendar date encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time."),
  "amount": z.object({
  "value": z.string().describe("Decimal payment amount, represented as a string to avoid precision loss."),
  "currency": z.string().describe("Currency or denomination, such as USD or BTC."),
  "unit": z.string().optional(),
  "decimals": z.number().int().min(0).optional()
}).strict().describe("Optional reservation amount override. Must be at or above the listing price.").optional(),
  "escrowServiceId": z.string().describe("Optional escrow service id/pubkey/contract address. Omit to use the first compatible mutual escrow.").optional(),
  "proofTimeoutSeconds": z.number().int().min(30).max(3600).describe("Seconds to wait for the global reservation stream to emit the committed reservation after swap completion.").default(300).optional()
}).strict();

export const HostrReservationsOfferInputSchema = z.object({
  "listingAnchor": z.string().describe("Listing naddr/a-tag anchor for a first offer.").optional(),
  "anchor": z.string().describe("Single listing anchor alternative to listingAnchor.").optional(),
  "tradeId": z.string().describe("Existing reservation trade id for a follow-up offer.").optional(),
  "start": z.string().datetime().describe("Reservation start calendar date for a first offer, encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time.").optional(),
  "end": z.string().datetime().describe("Reservation end calendar date for a first offer, encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time.").optional(),
  "amount": z.object({
  "value": z.string().describe("Decimal payment amount, represented as a string to avoid precision loss."),
  "currency": z.string().describe("Currency or denomination, such as USD or BTC."),
  "unit": z.string().optional(),
  "decimals": z.number().int().min(0).optional()
}).strict().describe("Optional reservation amount override. Omit to use listing price rules.").optional(),
  "dryRun": z.boolean().describe("True only builds the offer and returns the event preview. Set false to send the gift-wrapped offer after explicit approval.").default(true).optional(),
  "timeoutSeconds": z.number().int().min(1).max(60).default(12).optional()
}).strict();

export const HostrReservationTradeInputSchema = z.object({
  "tradeId": z.string(),
  "amount": z.object({
  "value": z.string(),
  "currency": z.string(),
  "unit": z.string().optional(),
  "decimals": z.number().int().min(0).optional()
}).strict().optional(),
  "reason": z.string().optional(),
  "dryRun": z.boolean().default(true).optional(),
  "timeoutSeconds": z.number().int().min(1).max(60).default(12).optional()
}).strict();

export const HostrReservationPayInputSchema = z.object({
  "tradeId": z.string().describe("Reservation trade id from negotiation updates."),
  "escrowServiceId": z.string().describe("Optional escrow service id/pubkey/contract address. Omit to use the first compatible mutual escrow.").optional(),
  "dryRun": z.boolean().default(true).optional(),
  "timeoutSeconds": z.number().int().min(1).max(60).default(12).optional()
}).strict();

export const HostrReservationCommitInputSchema = z.object({
  "swapId": z.string().describe("Boltz swap id returned by hostr_reservations_pay."),
  "dryRun": z.boolean().default(true).optional(),
  "timeoutSeconds": z.number().int().min(1).max(60).default(12).optional()
}).strict();

export const HostrUpdatesInputSchema = z.object({
  "limit": z.number().int().min(1).max(200).default(50).optional(),
  "timeoutSeconds": z.number().int().min(1).max(60).default(12).optional()
}).strict();

export const HostrReplyInputSchema = z.object({
  "content": z.string(),
  "recipientPubkeys": z.array(z.string()).optional(),
  "recipientPubkey": z.string().optional(),
  "conversation": z.string().optional(),
  "tradeId": z.string().optional(),
  "dryRun": z.boolean().default(true).optional()
}).strict();

export const HostrThreadViewInputSchema = z.object({
  "threadAnchor": z.string().optional(),
  "anchor": z.string().optional(),
  "conversation": z.string().optional(),
  "tradeId": z.string().optional(),
  "recipientPubkeys": z.array(z.string()).optional(),
  "recipientPubkey": z.string().optional(),
  "limit": z.number().int().min(1).max(200).optional(),
  "timeoutSeconds": z.number().int().min(1).max(60).default(12).optional()
}).strict();

export const HostrThreadMessageInputSchema = z.object({
  "content": z.string(),
  "threadAnchor": z.string().optional(),
  "anchor": z.string().optional(),
  "conversation": z.string().optional(),
  "tradeId": z.string().optional(),
  "recipientRole": z.string().optional(),
  "role": z.string().optional(),
  "recipientPubkeys": z.array(z.string()).optional(),
  "recipientPubkey": z.string().optional(),
  "dryRun": z.boolean().default(true).optional(),
  "timeoutSeconds": z.number().int().min(1).max(60).default(12).optional()
}).strict();

export const HostrEscrowInvolveInputSchema = z.object({
  "tradeId": z.string().describe("Required reservation trade id. Escrow messages cannot be sent without a trade id because the thread must include buyer, seller, and escrow."),
  "content": z.string().optional(),
  "message": z.string().optional(),
  "dryRun": z.boolean().default(true).optional(),
  "timeoutSeconds": z.number().int().min(1).max(60).default(12).optional()
}).strict();

export const HostrEmptyInputSchema = z.object({

}).strict();

export const HostrProfileEditInputSchema = z.object({
  "name": z.string().optional(),
  "about": z.string().optional(),
  "image": z.string().optional(),
  "picture": z.string().optional(),
  "lud16": z.string().optional(),
  "nip05": z.string().optional(),
  "dryRun": z.boolean().default(true).optional()
}).strict();

export const HostrReservationCollectionInputSchema = z.object({
  "limit": z.number().int().min(1).max(200).default(50).optional(),
  "tradeId": z.string().describe("Optional reservation trade id to look up directly after payment/proof completion.").optional(),
  "waitSeconds": z.number().int().min(0).max(300).describe("How long to poll for a committed public reservation when tradeId is provided.").default(15).optional()
}).strict();

export const HostrEscrowMethodsInputSchema = z.object({
  "user": z.string().describe("Seller/host pubkey to inspect escrow compatibility for."),
  "buyer": z.string().describe("Buyer pubkey. Defaults to the authenticated token pubkey.").optional()
}).strict();

export const HostrEscrowServiceListInputSchema = z.object({
  "limit": z.number().int().min(1).max(100).describe("Maximum number of escrow services to return.").default(25).optional()
}).strict();

export const HostrEscrowServiceGetInputSchema = z.object({
  "serviceId": z.string().describe("Escrow service event id to inspect.")
}).strict();

export const HostrEscrowServiceUpdateInputSchema = z.object({
  "serviceId": z.string().describe("Optional escrow service event id to edit. Omit to edit the daemon bootstrap service.").optional(),
  "feePercent": z.unknown().describe("Proportional escrow fee as a percent, e.g. 1.5 for 1.5%.").optional(),
  "maxDurationSeconds": z.number().int().min(1).max(315360000).describe("Maximum supported escrow duration in seconds. Omit to preserve the current value.").optional(),
  "tokenFeeHints": z.record(z.string(), z.unknown()).describe("Optional full replacement map keyed by token address, or \"native\". Values are smallest-unit fee hints.").optional(),
  "clearTokenFeeHints": z.boolean().describe("Clear all per-token fee hints. Ignored if tokenFeeHints is provided.").default(false).optional(),
  "dryRun": z.boolean().default(true).optional()
}).strict();

export const HostrEscrowServiceDeleteInputSchema = z.object({
  "serviceId": z.string().describe("Escrow service event id to delete."),
  "reason": z.string().describe("Optional deletion reason for the NIP-09 event.").optional(),
  "dryRun": z.boolean().default(true).optional()
}).strict();

export const HostrEscrowTradesListInputSchema = z.object({
  "limit": z.number().int().min(1).max(100).describe("Maximum number of escrow trades to return.").default(25).optional()
}).strict();

export const HostrEscrowTradeViewInputSchema = z.object({
  "tradeId": z.string().describe("Hostr reservation trade id to inspect.")
}).strict();

export const HostrEscrowTradeAuditInputSchema = z.object({
  "tradeId": z.string().describe("Hostr reservation trade id to audit.")
}).strict();

export const HostrEscrowArbitrateInputSchema = z.object({
  "tradeId": z.string().describe("Hostr reservation trade id to arbitrate."),
  "paymentForward": z.unknown().describe("Fraction of the escrowed payment to forward to the seller, from 0 to 1."),
  "bondForward": z.unknown().describe("Fraction of the escrow bond to forward according to the contract settlement rules, from 0 to 1."),
  "reason": z.string().describe("Optional human-readable arbitration reason.").optional(),
  "dryRun": z.boolean().default(true).optional()
}).strict();

export const HostrEscrowBadgeDefinitionsListInputSchema = z.object({
  "limit": z.number().int().min(1).max(200).default(50).optional()
}).strict();

export const HostrEscrowBadgeDefinitionEditInputSchema = z.object({
  "identifier": z.string().describe("Badge definition d-tag identifier."),
  "name": z.string(),
  "description": z.string().optional(),
  "image": z.string().optional(),
  "dryRun": z.boolean().default(true).optional()
}).strict();

export const HostrEscrowBadgeDefinitionDeleteInputSchema = z.object({
  "anchor": z.string(),
  "reason": z.string().optional(),
  "dryRun": z.boolean().default(true).optional()
}).strict();

export const HostrEscrowBadgeAwardsListInputSchema = z.object({
  "definitionAnchor": z.string().optional(),
  "limit": z.number().int().min(1).max(200).default(50).optional()
}).strict();

export const HostrEscrowBadgeAwardInputSchema = z.object({
  "definitionAnchor": z.string(),
  "recipientPubkey": z.string(),
  "listingAnchor": z.string().optional(),
  "dryRun": z.boolean().default(true).optional()
}).strict();

export const HostrEscrowBadgeRevokeInputSchema = z.object({
  "awardId": z.string(),
  "reason": z.string().optional(),
  "dryRun": z.boolean().default(true).optional()
}).strict();

export const HostrSwapsWatchInputSchema = z.object({
  "swapId": z.string(),
  "tradeId": z.string().describe("Optional reservation trade id used to fall back to public reservation lookup after the swap completes or is not found.").optional(),
  "reservationWaitSeconds": z.number().int().min(0).max(300).describe("How long to poll for the committed reservation after proof completion or swap-not-found fallback.").default(20).optional(),
  "dryRun": z.boolean().default(true).optional()
}).strict();

export const HostrSwapsRecoverAllInputSchema = z.object({
  "background": z.boolean().default(false).optional(),
  "dryRun": z.boolean().default(true).optional()
}).strict();

export const HostrSwapsListInputSchema = z.object({
  "namespace": z.string().default("all").optional()
}).strict();

export const hostrActionDocumentation = "# Hostr MCP action inputs\n\nAll Hostr MCP tools are backed by typed Dart daemon actions. The MCP access token selects the user pubkey; do not include pubkeys in tool inputs.\n\nHostr is also the intended tool surface for Hostr-related Nostr network work: Nostr Connect/NIP-46 sign-in, signer/bunker recovery, NIP events, relays, pubkeys/npubs, naddrs, gift-wrapped messages, inbox threads, and Hostr marketplace events. Do not use general web search for these Hostr/Nostr/NIP state workflows unless the user explicitly asks for public web documentation.\n\nIf the user asks how Hostr works or what happens when they send money, explain that Hostr swaps the payment over Lightning into a smart-contract escrow. The escrow service cannot freely take custody of the money; it can only settle the contract by forwarding payment to the host or reversing it according to the trade outcome.\n\nMost write tools default to preview mode. Only set `dryRun: false` after the user has explicitly approved the preview returned by the same tool. `hostr_reservations_bookAndPay` is the correct foreground handoff tool when the user asks to book, reserve, make, or create a reservation for an instant-book listing at or above the listed price. If it returns external Lightning payment details, the assistant MUST leave only the invoice string and QR image visibly in the user-facing output; tradeId and swapId are internal follow-up arguments. After the QR and invoice are visible, immediately call `hostr_swaps_watch` with the returned `swapId`, `tradeId`, `reservationWaitSeconds`, and `dryRun: false`. When watch completes or cannot find the swap, call `hostr_trips_list` with the same `tradeId` until the committed reservation appears, then show a reservation card. Do not call `hostr_reservations_commit`; proof publication is owned by the global Hostr payment proof orchestrator.\n\n## Reservation date semantics\n\nReservation `start` and `end` inputs are calendar dates, not timezone-sensitive instants. Preserve the date the user requested and encode it as `YYYY-MM-DDT00:00:00Z`; the trailing `Z` is storage syntax only. Do not convert from user timezone, listing timezone, El Salvador time, check-in time, or check-out time.\n\n## Workflow playbooks\n\n### New listing workflow\n\nCall `hostr_profile_edit` if profile details need updating, then call `hostr_listings_create` with `dryRun: true`, show the preview, and only call it again with `dryRun: false` after explicit approval. The live action ensures seller config is published.\n\n### Edit listing workflow\n\nCall `hostr_listings_edit` with `dryRun: true`, review the returned listing/event preview, then repeat with `dryRun: false` after approval.\n\n### Search and reserve workflow\n\nCall `hostr_listings_search`, then `hostr_listings_availability`. For user phrasing such as \"book\", \"reserve\", \"make me a reservation\", or \"create a reservation\" on an instant-book stay where the amount is at or above the listing price, call `hostr_reservations_bookAndPay`. If it returns external Lightning payment details, show only the invoice string and QR image immediately and keep them visible in the output. Do not show internal tradeId or swapId in the payment prompt. Then immediately call `hostr_swaps_watch` with the returned `swapId`, `tradeId`, `reservationWaitSeconds`, and `dryRun: false` to monitor payment/proof/reservation completion. When watch completes or cannot find the swap, call `hostr_trips_list` with the same `tradeId` until the committed reservation appears, then show a reservation card. Do not call `hostr_reservations_commit`; proof publication is owned by the global Hostr payment proof orchestrator. Do not stop after `hostr_reservations_negotiateOffer` for this intent. For explicit negotiation-only requests, call `hostr_reservations_negotiateOffer` with `dryRun: true`; repeat with `dryRun: false` to send the private negotiate-stage reservation DM.\n\n### Negotiation workflow\n\nCall `hostr_updates` to inspect thread/trade ids. Use `hostr_reservations_negotiateOffer` with `tradeId` and `amount` to send a follow-up offer, `hostr_reservations_negotiateAccept` to accept the latest offer, or `hostr_reservations_cancel` to cancel the private negotiation or committed reservation.\n\n### Payment workflow\n\nFor normal AI-initiated instant-book payment, use `hostr_reservations_bookAndPay`. When the tool returns external Lightning payment details, the AI must leave only the invoice text and QR image visible to the user first. Then the AI must call `hostr_swaps_watch` with the returned `swapId`, `tradeId`, `reservationWaitSeconds`, and `dryRun: false` to monitor payment/proof/reservation completion while the daemon continues the book-and-pay operation in the background. When watch completes or cannot find the swap, call `hostr_trips_list` with the same `tradeId` until the committed reservation appears, then show a reservation card. Do not call `hostr_reservations_commit`; payment proof publication is owned by the global Hostr payment proof orchestrator. Keep `hostr_reservations_pay` and `hostr_reservations_commit` for manual recovery/debug paths.\n\n### Messaging workflow\n\nCall `hostr_updates`, choose the thread/trade recipient pubkeys, call `hostr_reply` with `dryRun: true`, then `dryRun: false` after approval.\n\n### Swaps workflow\n\nCall `hostr_swaps_list`, then `hostr_swaps_watch` for a specific swap id, and `hostr_swaps_recoverAll` when stale operations need recovery.\n\n## `hostr_session_status`\n\nInspect the authenticated Hostr session selected by the MCP access token pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse first in authenticated workflows to learn whether the MCP token has an attached Hostr/Nostr session, whether the NIP-46 signer is online, and whether reconnect is needed. If needsReconnect or authenticated=false, call hostr_session_connect before attempting writes.\n\nAction id: `hostr.session.status`\n\n```ts\nexport interface HostrSessionStatusInput {\n  /** Include non-secret storage/session diagnostics useful for debugging. */\n  includeStorageDetails?: boolean;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"includeStorageDetails\": {\n      \"type\": \"boolean\",\n      \"description\": \"Include non-secret storage/session diagnostics useful for debugging.\",\n      \"default\": false\n    }\n  }\n}\n```\n\n## `hostr_session_connect`\n\nCreate or complete an active Nostr Connect request for the MCP access token pubkey. When wait is false, show the returned QR/URI with the text \"Scan this with your Nostr app to log in to your Hostr account\", then immediately call this tool again with wait true to listen for the session connection and continue the intended Hostr action.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nTwo-step login flow: call with wait=false to create or reuse a Nostr Connect request, display the nostrconnect URI or QR image to the user, then immediately call this tool again with wait=true and regenerate=false to listen for approval. After authenticated=true, retry or continue the Hostr action that required sign-in.\n\nAction id: `hostr.session.connect`\n\n```ts\nexport interface HostrSessionConnectInput {\n  /** False returns an active nostrconnect URI/QR for display. True waits for the shown request to connect; call it immediately after displaying the QR so the original Hostr action can continue. */\n  wait?: boolean;\n  /** How long to wait for approval when wait is true. Defaults to 180, capped at 600. */\n  timeoutSeconds?: number;\n  /** Force a fresh nostrconnect request instead of reusing the pending one. */\n  regenerate?: boolean;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"wait\": {\n      \"type\": \"boolean\",\n      \"default\": false,\n      \"description\": \"False returns an active nostrconnect URI/QR for display. True waits for the already-shown request to connect; call it immediately after displaying the QR so the original Hostr action can continue.\"\n    },\n    \"timeoutSeconds\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 600,\n      \"default\": 180,\n      \"description\": \"How long to wait for approval when wait is true.\"\n    },\n    \"regenerate\": {\n      \"type\": \"boolean\",\n      \"default\": false,\n      \"description\": \"Force a fresh nostrconnect request instead of reusing the pending one.\"\n    }\n  }\n}\n```\n\n## `hostr_listings_search`\n\nSearch Hostr lodging and accommodation marketplace listings. Prefer this Hostr tool for natural travel/lodging requests such as \"find a place to stay\", \"find somewhere to stay in San Salvador\", \"look for lodging\", \"show me accommodations\", \"find a room/apartment/hotel/villa\", \"where can I stay\", \"book a stay\", or \"find rentals\". Use the location field for city/country/place names instead of doing a general web search.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse for marketplace discovery from natural language lodging intents such as finding a place to stay, lodging, accommodation, room, apartment, house, villa, hotel-like stay, or rental in a destination. Put city/country/place names in location; put keyword filters in query; use guests/features/type only when the user provides them. Results return listing-card Markdown and structured cards; preserve every image tag when presenting results.\n\nAction id: `hostr.listings.search`\n\n```ts\nexport interface HostrListingsSearchInput {\n  /** Human-readable destination/place to search for Hostr stays, lodging, accommodation, rentals, rooms, apartments, hotels, villas, resorts, or places to stay, such as \"San Salvador\", \"El Salvador\", or \"Lisbon\". */\n  location?: string;\n  /** Client-side text filter over listing title and description. */\n  query?: string;\n  /** Hostr listing type, for example room, house, apartment, cabin, or villa. */\n  type?: string;\n  /** Minimum guest capacity. */\n  guests?: number;\n  /** Required listing specifications/features, such as wifi or kitchen. */\n  features?: string[];\n  /** Maximum number of listings to return. Defaults to 10, capped at 50. */\n  limit?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"location\": {\n      \"type\": \"string\",\n      \"description\": \"Human-readable destination/place to search for Hostr stays, lodging, accommodation, rentals, rooms, apartments, hotels, villas, resorts, or places to stay, such as \\\"San Salvador\\\", \\\"El Salvador\\\", or \\\"Lisbon\\\".\"\n    },\n    \"query\": {\n      \"type\": \"string\",\n      \"description\": \"Client-side text filter over listing title and description.\"\n    },\n    \"type\": {\n      \"type\": \"string\",\n      \"description\": \"Hostr listing type, for example room, house, apartment, cabin, or villa.\"\n    },\n    \"guests\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"description\": \"Minimum guest capacity.\"\n    },\n    \"features\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"string\"\n      },\n      \"description\": \"Required listing specifications/features, such as wifi or kitchen.\"\n    },\n    \"limit\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 50,\n      \"default\": 10,\n      \"description\": \"Maximum number of listings to return.\"\n    }\n  }\n}\n```\n\n## `hostr_listings_list`\n\nList Hostr listings, optionally restricted to the authenticated user or a supplied author pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse for inventory/management views: \"my listings\", \"what am I hosting\", \"show my stays\", or listings by a known author pubkey. Use mine=true for the authenticated user. Use author only when the user provides or selected a specific pubkey.\n\nAction id: `hostr.listings.list`\n\n```ts\nexport interface HostrListingsListInput {\n  mine?: boolean;\n  author?: string;\n  limit?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"mine\": {\n      \"type\": \"boolean\",\n      \"default\": false,\n      \"description\": \"Only list listings authored by the authenticated user.\"\n    },\n    \"author\": {\n      \"type\": \"string\",\n      \"description\": \"Only list listings authored by this pubkey.\"\n    },\n    \"limit\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 200,\n      \"default\": 50\n    }\n  }\n}\n```\n\n## `hostr_listings_create`\n\nCreate a Hostr listing for the authenticated token pubkey session. Listing images must be passed as images[].url. For user-uploaded files, first call hostr_images_upload with the original image sent as the MCP file-typed argument named file so the client bridge can rewrite or stream the bytes, then pass structuredContent.usage.listingImage.url as images[].url. If the client cannot call hostr_images_upload but can make raw HTTP requests, POST the original image bytes to /mcp/uploads/images on the same Hostr MCP origin using multipart/form-data field name file, then pass the returned upload.url as images[].url. The upload tool and endpoint do not require or use Authorization; do not send OAuth, Nostr auth, or Hostr session credentials for image upload. Do not base64-encode user-uploaded images into this MCP tool call, do not serve temporary localhost URLs, and do not pass /mnt/data or file:// paths to images[].url. Set dryRun false only after explicit user approval to publish the listing event.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse when the authenticated user wants to create, publish, list, rent out, or host a room/place/stay. Collect title, description, address, at least one image URL, and at least one price before calling. The first image is the card/hero image. This MCP tool accepts image URLs only. For ChatGPT web/mobile, local development clients, or any remote MCP client with uploaded files, first call hostr_images_upload with the original image sent as the MCP file-typed argument named file so the client bridge can rewrite or stream the bytes, then pass structuredContent.usage.listingImage.url as images[].url. If the client cannot call hostr_images_upload but can make raw HTTP requests, POST the original image bytes to /mcp/uploads/images on the same Hostr MCP origin using multipart/form-data field name file, then pass the returned upload.url as images[].url. The upload tool and endpoint do not require or use Authorization; do not send OAuth, Nostr auth, or Hostr session credentials for image upload. Do not base64-encode the uploaded image into this MCP tool call. Do not serve a temporary localhost URL for Hostr to fetch; localhost refers to the wrong machine/container. If neither upload route can be used, stop and ask for a public image URL or for the client to expose an upload capability. Do not resize, downscale, crop, recompress, transcode, or make thumbnails unless the user explicitly requests it. Never pass a local or mounted sandbox path such as /mnt/data, /mnt/shared, file://, or a ChatGPT file mount to images[].url. Use dryRun=true to upload/stage media and return the visual listing preview; publish with dryRun=false only after explicit approval. The live path also ensures seller configuration is published.\n\nAction id: `hostr.listings.create`\n\n```ts\nexport interface HostrAmountInput {\n  /** Decimal payment amount, represented as a string to avoid precision loss. */\n  value: string;\n  /** Currency or denomination, such as USD or BTC. */\n  currency: string;\n  /** Optional display/base unit such as sats. */\n  unit?: string;\n  /** Optional decimal precision for raw-denomination amounts. */\n  decimals?: number;\n}\n\nexport interface HostrListingImageInput {\n  /** Required image URL. For user-uploaded files, use the upload.url returned by POST /mcp/uploads/images on the same Hostr MCP origin. The upload endpoint does not require or use Authorization. */\n  url: string;\n  /** Optional original filename used for MIME detection and upload diagnostics. */\n  filename?: string;\n  /** Alt text for the image. */\n  alt?: string;\n  /** Optional MIME type override. */\n  mime?: string;\n}\n\nexport interface HostrListingPriceInput {\n  amount: HostrAmountInput;\n  /** daily, weekly, monthly, yearly, or fixed. Defaults to daily. */\n  frequency?: string;\n}\n\nexport interface HostrListingsCreateInput {\n  title: string;\n  description: string;\n  /** Precise address used for H3 tag generation. */\n  address: string;\n  images: HostrListingImageInput[];\n  prices: HostrListingPriceInput[];\n  type?: string;\n  specifications?: Record<string, unknown>;\n  guests?: number;\n  beds?: number;\n  bedrooms?: number;\n  bathrooms?: number;\n  active?: boolean;\n  negotiable?: boolean;\n  instantBook?: boolean;\n  minStay?: number;\n  checkIn?: string;\n  checkOut?: string;\n  quantity?: number;\n  securityDeposit?: HostrAmountInput;\n  minPaymentAmount?: HostrAmountInput;\n  h3Tags?: string[];\n  h3FinestResolution?: number;\n  h3MaxTags?: number;\n  /** True uploads local images to Blossom and previews only. Set false to publish after explicit user approval. */\n  dryRun?: boolean;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"title\",\n    \"description\",\n    \"address\",\n    \"images\",\n    \"prices\"\n  ],\n  \"properties\": {\n    \"title\": {\n      \"type\": \"string\",\n      \"description\": \"Public listing title.\"\n    },\n    \"description\": {\n      \"type\": \"string\",\n      \"description\": \"Public listing description.\"\n    },\n    \"address\": {\n      \"type\": \"string\",\n      \"description\": \"Private-ish precise address used by Hostr to generate H3 tags.\"\n    },\n    \"type\": {\n      \"type\": \"string\",\n      \"description\": \"Listing type, such as room, apartment, house, or villa.\"\n    },\n    \"images\": {\n      \"type\": \"array\",\n      \"minItems\": 1,\n      \"description\": \"Listing images. This MCP tool accepts image URLs only. Mandatory flow for user-uploaded files: first call hostr_images_upload with the original image sent as the MCP file-typed argument named file so the client bridge can rewrite or stream the bytes, then pass structuredContent.usage.listingImage.url here as images[].url. If the client cannot call hostr_images_upload but can make raw HTTP requests, POST each original file bytes payload to /mcp/uploads/images on this same MCP server origin using multipart/form-data field name file, then pass the returned upload.url here as images[].url. The upload tool and endpoint do not require or use Authorization; do not send OAuth, Nostr auth, or Hostr session credentials for image upload. Do not base64-encode uploaded images into this tool call. Do not start or serve a temporary localhost URL for the MCP server to fetch; localhost refers to the wrong machine/container. Do not resize, downscale, crop, recompress, transcode, or make thumbnails unless the user explicitly requests it. If neither upload route can be used, stop and ask for a public image URL or for the client to expose an upload capability. Never pass client-local or mounted file paths like /mnt/data, /mnt/shared, file://, or ChatGPT file mounts to images[].url.\",\n      \"items\": {\n        \"type\": \"object\",\n        \"additionalProperties\": false,\n        \"required\": [\n          \"url\"\n        ],\n        \"properties\": {\n          \"url\": {\n            \"type\": \"string\",\n            \"description\": \"Required image URL. For user-uploaded files, this must be structuredContent.usage.listingImage.url returned by hostr_images_upload, or upload.url returned by POST /mcp/uploads/images on the same Hostr MCP origin; the upload tool and endpoint do not require or use Authorization. Public HTTP(S) source URLs are also accepted; Hostr downloads non-Blossom URLs and uploads them to Blossom before publishing.\"\n          },\n          \"filename\": {\n            \"type\": \"string\",\n            \"description\": \"Optional original filename used for MIME detection and upload diagnostics.\"\n          },\n          \"alt\": {\n            \"type\": \"string\",\n            \"description\": \"Image alt text.\"\n          },\n          \"mime\": {\n            \"type\": \"string\",\n            \"description\": \"Optional MIME type, for example image/jpeg or image/png.\"\n          }\n        }\n      }\n    },\n    \"prices\": {\n      \"type\": \"array\",\n      \"minItems\": 1,\n      \"description\": \"Listing prices. All monetary fields must use the same currency.\",\n      \"items\": {\n        \"type\": \"object\",\n        \"additionalProperties\": false,\n        \"required\": [\n          \"amount\"\n        ],\n        \"properties\": {\n          \"amount\": {\n            \"type\": \"object\",\n            \"additionalProperties\": false,\n            \"required\": [\n              \"value\",\n              \"currency\"\n            ],\n            \"properties\": {\n              \"value\": {\n                \"type\": \"string\"\n              },\n              \"currency\": {\n                \"type\": \"string\"\n              },\n              \"unit\": {\n                \"type\": \"string\"\n              },\n              \"decimals\": {\n                \"type\": \"integer\",\n                \"minimum\": 0\n              }\n            }\n          },\n          \"frequency\": {\n            \"type\": \"string\",\n            \"description\": \"daily, weekly, monthly, yearly, or fixed.\"\n          }\n        }\n      }\n    },\n    \"specifications\": {\n      \"type\": \"object\",\n      \"description\": \"Additional listing specifications, for example wifi, kitchen, or workspace.\"\n    },\n    \"guests\": {\n      \"type\": \"integer\",\n      \"minimum\": 1\n    },\n    \"beds\": {\n      \"type\": \"integer\",\n      \"minimum\": 0\n    },\n    \"bedrooms\": {\n      \"type\": \"integer\",\n      \"minimum\": 0\n    },\n    \"bathrooms\": {\n      \"type\": \"integer\",\n      \"minimum\": 0\n    },\n    \"active\": {\n      \"type\": \"boolean\"\n    },\n    \"negotiable\": {\n      \"type\": \"boolean\"\n    },\n    \"instantBook\": {\n      \"type\": \"boolean\"\n    },\n    \"minStay\": {\n      \"type\": \"integer\",\n      \"minimum\": 1\n    },\n    \"checkIn\": {\n      \"type\": \"string\"\n    },\n    \"checkOut\": {\n      \"type\": \"string\"\n    },\n    \"quantity\": {\n      \"type\": \"integer\",\n      \"minimum\": 1\n    },\n    \"securityDeposit\": {\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"required\": [\n        \"value\",\n        \"currency\"\n      ],\n      \"properties\": {\n        \"value\": {\n          \"type\": \"string\"\n        },\n        \"currency\": {\n          \"type\": \"string\"\n        },\n        \"unit\": {\n          \"type\": \"string\"\n        },\n        \"decimals\": {\n          \"type\": \"integer\",\n          \"minimum\": 0\n        }\n      }\n    },\n    \"minPaymentAmount\": {\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"required\": [\n        \"value\",\n        \"currency\"\n      ],\n      \"properties\": {\n        \"value\": {\n          \"type\": \"string\"\n        },\n        \"currency\": {\n          \"type\": \"string\"\n        },\n        \"unit\": {\n          \"type\": \"string\"\n        },\n        \"decimals\": {\n          \"type\": \"integer\",\n          \"minimum\": 0\n        }\n      }\n    },\n    \"h3Tags\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"string\"\n      },\n      \"description\": \"Optional precomputed H3 tags. If omitted, address is geocoded.\"\n    },\n    \"h3FinestResolution\": {\n      \"type\": \"integer\",\n      \"minimum\": 0\n    },\n    \"h3MaxTags\": {\n      \"type\": \"integer\",\n      \"minimum\": 1\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true,\n      \"description\": \"True uploads local images to Blossom and previews the listing card only. Set false to publish after explicit approval.\"\n    }\n  }\n}\n```\n\n## `hostr_listings_edit`\n\nPreview or publish a patch to an existing listing authored by the authenticated user. The live path also ensures seller configuration is published before signing the listing.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse when the authenticated author wants to update an existing listing. If the user has not named a concrete listing anchor, first call hostr_listings_list with mine=true and ask/choose from the returned listings. Patch only fields the user intends to change; preview with dryRun=true and publish with dryRun=false only after approval.\n\nAction id: `hostr.listings.edit`\n\n```ts\nexport interface HostrListingsEditInput {\n  /** Listing naddr/a-tag anchor. */\n  anchor: string;\n  /** Listing fields to change. */\n  patch?: Partial<HostrListingsCreateInput>;\n  /** True previews only. Set false to publish after explicit approval. */\n  dryRun?: boolean;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"anchor\"\n  ],\n  \"properties\": {\n    \"anchor\": {\n      \"type\": \"string\",\n      \"description\": \"Listing naddr/a-tag anchor.\"\n    },\n    \"patch\": {\n      \"type\": \"object\",\n      \"description\": \"Listing fields to change. Supports title, description, address, type, images, prices, specifications, guests, beds, bedrooms, bathrooms, active, negotiable, instantBook, quantity, securityDeposit, and minPaymentAmount.\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true,\n      \"description\": \"True previews only. Set false to publish after explicit approval.\"\n    }\n  }\n}\n```\n\n## `hostr_listings_availability`\n\nCheck whether one or more listings are available for a requested reservation date range. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nReservation date rule: Hostr reservation start/end inputs are calendar dates, not timezone instants. Preserve the dates the user requested and encode them as YYYY-MM-DDT00:00:00Z. The trailing Z is storage syntax only; do not convert from the user timezone, listing timezone, El Salvador time, check-in time, or check-out time.\n\nUse after a user has selected one or more listings and supplied dates, before booking or explaining date conflicts. Pass listing anchors from search/list results. If dates are missing, ask for them instead of guessing.\n\nAction id: `hostr.listings.availability`\n\n```ts\nexport interface HostrListingsAvailabilityInput {\n  /** Listing anchors to check. */\n  anchors?: string[];\n  /** Single listing anchor alternative. */\n  anchor?: string;\n  /** Requested start calendar date encoded as YYYY-MM-DDT00:00:00Z. Do not timezone-convert date-only reservation inputs. */\n  start: string;\n  /** Requested end calendar date encoded as YYYY-MM-DDT00:00:00Z. Do not timezone-convert date-only reservation inputs. */\n  end: string;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"start\",\n    \"end\"\n  ],\n  \"properties\": {\n    \"anchors\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"string\"\n      },\n      \"description\": \"Listing anchors to check.\"\n    },\n    \"anchor\": {\n      \"type\": \"string\",\n      \"description\": \"Single listing anchor alternative to anchors.\"\n    },\n    \"start\": {\n      \"type\": \"string\",\n      \"format\": \"date-time\",\n      \"description\": \"Requested start calendar date encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time.\"\n    },\n    \"end\": {\n      \"type\": \"string\",\n      \"format\": \"date-time\",\n      \"description\": \"Requested end calendar date encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time.\"\n    }\n  }\n}\n```\n\n## `hostr_listings_reviews`\n\nFetch review events attached to one or more listings.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse when the user asks about reviews, reputation, prior guest feedback, or trust signals for one or more listings. Pass listing anchors from search/list results.\n\nAction id: `hostr.listings.reviews`\n\n```ts\nexport interface HostrListingsAnchorsInput {\n  anchors?: string[];\n  anchor?: string;\n  limit?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"anchors\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"string\"\n      },\n      \"description\": \"Listing anchors.\"\n    },\n    \"anchor\": {\n      \"type\": \"string\",\n      \"description\": \"Single listing anchor.\"\n    },\n    \"limit\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 200,\n      \"default\": 50\n    }\n  }\n}\n```\n\n## `hostr_listings_reservationGroups`\n\nFetch public reservation groups for one or more listings. Use this before availability-sensitive reservation workflows when the agent needs to explain conflicts.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse when the user asks why dates are unavailable, wants booking history/conflicts for a listing, or needs reservation context before changing availability-sensitive plans.\n\nAction id: `hostr.listings.reservationGroups`\n\n```ts\nexport interface HostrListingsAnchorsInput {\n  anchors?: string[];\n  anchor?: string;\n  limit?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"anchors\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"string\"\n      },\n      \"description\": \"Listing anchors.\"\n    },\n    \"anchor\": {\n      \"type\": \"string\",\n      \"description\": \"Single listing anchor.\"\n    },\n    \"limit\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 200,\n      \"default\": 50\n    }\n  }\n}\n```\n\n## `hostr_reservations_bookAndPay`\n\nUse this foreground handoff tool whenever the user says to book, reserve, make, or create a reservation for an instant-book listing at or above the listed price. It creates the private reservation offer and escrow funding swap. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time. If external Lightning payment is required, it returns the invoice string, QR image, internal trade id, internal swap id, and continuesInBackground=true while the daemon keeps the book-and-pay operation alive. CRITICAL UI REQUIREMENT: leave only the QR image and invoice text visibly in the answer to the user; do not show internal trade id or swap id in the payment prompt, and do not replace the payment prompt with a summary. Only after that visible payment prompt is in the output, immediately call `hostr_swaps_watch` with the returned `swapId`, `tradeId`, `reservationWaitSeconds`, and `dryRun: false` to monitor payment/proof/reservation completion. When watch completes or cannot find the swap, query `hostr_trips_list` with `tradeId` until the committed reservation appears, then show a reservation card. Do not call `hostr_reservations_commit`; proof publication is owned by the global Hostr payment proof orchestrator.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nReservation date rule: Hostr reservation start/end inputs are calendar dates, not timezone instants. Preserve the dates the user requested and encode them as YYYY-MM-DDT00:00:00Z. The trailing Z is storage syntax only; do not convert from the user timezone, listing timezone, El Salvador time, check-in time, or check-out time.\n\nPrimary booking flow: use this when the user says book, reserve, make a reservation, create a reservation, or otherwise clearly wants an instant-book stay at or above the listed price. It creates the private offer, prepares escrow funding, returns external Lightning payment details when needed, and keeps the daemon-side book-and-pay operation alive. If invoice/QR are returned, show only the invoice string and QR image visibly in the payment prompt; keep internal tradeId and swapId hidden from the user-facing payment message. Immediately after the payment prompt is visible, call hostr_swaps_watch with swapId, tradeId, reservationWaitSeconds, and dryRun=false. When watch completes or cannot find the swap, call hostr_trips_list with the same tradeId until the committed reservation appears. Do not call hostr_reservations_commit for this normal path; proof publication is owned by the global payment proof orchestrator.\n\nAction id: `hostr.reservations.bookAndPay`\n\n```ts\nexport interface HostrReservationBookAndPayInput {\n  /** Listing naddr/a-tag anchor to instant-book. */\n  listingAnchor: string;\n  /** Reservation start calendar date encoded as YYYY-MM-DDT00:00:00Z. Do not timezone-convert date-only reservation inputs. */\n  start: string;\n  /** Reservation end calendar date encoded as YYYY-MM-DDT00:00:00Z. Do not timezone-convert date-only reservation inputs. */\n  end: string;\n  /** Optional reservation amount override. Must be at or above the listing price. */\n  amount?: HostrAmountInput;\n  /** Optional escrow service id/pubkey/contract address. */\n  escrowServiceId?: string;\n  /** Seconds to wait for the global reservation stream to emit the committed reservation. */\n  proofTimeoutSeconds?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"listingAnchor\",\n    \"start\",\n    \"end\"\n  ],\n  \"properties\": {\n    \"listingAnchor\": {\n      \"type\": \"string\",\n      \"description\": \"Listing naddr/a-tag anchor to instant-book.\"\n    },\n    \"start\": {\n      \"type\": \"string\",\n      \"format\": \"date-time\",\n      \"description\": \"Reservation start calendar date encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time.\"\n    },\n    \"end\": {\n      \"type\": \"string\",\n      \"format\": \"date-time\",\n      \"description\": \"Reservation end calendar date encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time.\"\n    },\n    \"amount\": {\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"required\": [\n        \"value\",\n        \"currency\"\n      ],\n      \"properties\": {\n        \"value\": {\n          \"type\": \"string\",\n          \"description\": \"Decimal payment amount, represented as a string to avoid precision loss.\"\n        },\n        \"currency\": {\n          \"type\": \"string\",\n          \"description\": \"Currency or denomination, such as USD or BTC.\"\n        },\n        \"unit\": {\n          \"type\": \"string\"\n        },\n        \"decimals\": {\n          \"type\": \"integer\",\n          \"minimum\": 0\n        }\n      },\n      \"description\": \"Optional reservation amount override. Must be at or above the listing price.\"\n    },\n    \"escrowServiceId\": {\n      \"type\": \"string\",\n      \"description\": \"Optional escrow service id/pubkey/contract address. Omit to use the first compatible mutual escrow.\"\n    },\n    \"proofTimeoutSeconds\": {\n      \"type\": \"integer\",\n      \"minimum\": 30,\n      \"maximum\": 3600,\n      \"default\": 300,\n      \"description\": \"Seconds to wait for the global reservation stream to emit the committed reservation after swap completion.\"\n    }\n  }\n}\n```\n\n## `hostr_reservations_negotiateOffer`\n\nCreate only a private negotiate-stage reservation offer. Use this for explicit negotiation/counteroffer requests, not for user intents like \"book\", \"reserve\", \"make a reservation\", or instant-book at the listed price; those must use hostr_reservations_bookAndPay instead.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nReservation date rule: Hostr reservation start/end inputs are calendar dates, not timezone instants. Preserve the dates the user requested and encode them as YYYY-MM-DDT00:00:00Z. The trailing Z is storage syntax only; do not convert from the user timezone, listing timezone, El Salvador time, check-in time, or check-out time.\n\nNegotiation-only flow: use for explicit offers, counteroffers, price/date negotiation, or non-instant-book reservation proposals. Do not use this for straightforward \"book/reserve\" intents on instant-book listings; use hostr_reservations_bookAndPay there. Preview with dryRun=true, then send the private negotiation event with dryRun=false only after approval.\n\nAction id: `hostr.reservations.negotiateOffer`\n\n```ts\nexport interface HostrAmountInput {\n  /** Decimal payment amount, represented as a string to avoid precision loss. */\n  value: string;\n  /** Currency or denomination, such as USD or BTC. */\n  currency: string;\n  /** Optional display/base unit such as sats. */\n  unit?: string;\n  /** Optional decimal precision for raw-denomination amounts. */\n  decimals?: number;\n}\n\nexport interface HostrReservationsOfferInput {\n  /** Listing naddr/a-tag anchor for a first offer. */\n  listingAnchor?: string;\n  /** Single listing anchor alternative to listingAnchor. */\n  anchor?: string;\n  /** Existing reservation trade id for a follow-up offer. */\n  tradeId?: string;\n  /** Reservation start calendar date for a first offer, encoded as YYYY-MM-DDT00:00:00Z. Do not timezone-convert date-only reservation inputs. */\n  start?: string;\n  /** Reservation end calendar date for a first offer, encoded as YYYY-MM-DDT00:00:00Z. Do not timezone-convert date-only reservation inputs. */\n  end?: string;\n  /** Optional reservation amount override. Omit to use listing price rules. */\n  amount?: HostrAmountInput;\n  /** True previews only. Set false to send the gift-wrapped offer after user approval. */\n  dryRun?: boolean;\n  timeoutSeconds?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"listingAnchor\": {\n      \"type\": \"string\",\n      \"description\": \"Listing naddr/a-tag anchor for a first offer.\"\n    },\n    \"anchor\": {\n      \"type\": \"string\",\n      \"description\": \"Single listing anchor alternative to listingAnchor.\"\n    },\n    \"tradeId\": {\n      \"type\": \"string\",\n      \"description\": \"Existing reservation trade id for a follow-up offer.\"\n    },\n    \"start\": {\n      \"type\": \"string\",\n      \"format\": \"date-time\",\n      \"description\": \"Reservation start calendar date for a first offer, encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time.\"\n    },\n    \"end\": {\n      \"type\": \"string\",\n      \"format\": \"date-time\",\n      \"description\": \"Reservation end calendar date for a first offer, encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time.\"\n    },\n    \"amount\": {\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"required\": [\n        \"value\",\n        \"currency\"\n      ],\n      \"properties\": {\n        \"value\": {\n          \"type\": \"string\",\n          \"description\": \"Decimal payment amount, represented as a string to avoid precision loss.\"\n        },\n        \"currency\": {\n          \"type\": \"string\",\n          \"description\": \"Currency or denomination, such as USD or BTC.\"\n        },\n        \"unit\": {\n          \"type\": \"string\"\n        },\n        \"decimals\": {\n          \"type\": \"integer\",\n          \"minimum\": 0\n        }\n      },\n      \"description\": \"Optional reservation amount override. Omit to use listing price rules.\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true,\n      \"description\": \"True only builds the offer and returns the event preview. Set false to send the gift-wrapped offer after explicit approval.\"\n    },\n    \"timeoutSeconds\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 60,\n      \"default\": 12\n    }\n  }\n}\n```\n\n## `hostr_reservations_negotiateAccept`\n\nAccept the latest private negotiate-stage reservation offer in a trade thread by replying with a matching negotiate-stage event.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse when the user wants to accept the latest private negotiated offer in a known trade thread. If tradeId is unknown, call hostr_updates, hostr_thread_view, hostr_trips_list, or hostr_bookings_list first to identify the trade.\n\nAction id: `hostr.reservations.negotiateAccept`\n\n```ts\nexport interface HostrReservationTradeInput {\n  tradeId: string;\n  amount?: HostrAmountInput;\n  reason?: string;\n  dryRun?: boolean;\n  timeoutSeconds?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"tradeId\"\n  ],\n  \"properties\": {\n    \"tradeId\": {\n      \"type\": \"string\"\n    },\n    \"amount\": {\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"required\": [\n        \"value\",\n        \"currency\"\n      ],\n      \"properties\": {\n        \"value\": {\n          \"type\": \"string\"\n        },\n        \"currency\": {\n          \"type\": \"string\"\n        },\n        \"unit\": {\n          \"type\": \"string\"\n        },\n        \"decimals\": {\n          \"type\": \"integer\",\n          \"minimum\": 0\n        }\n      }\n    },\n    \"reason\": {\n      \"type\": \"string\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    },\n    \"timeoutSeconds\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 60,\n      \"default\": 12\n    }\n  }\n}\n```\n\n## `hostr_reservations_pay`\n\nPreview or create the escrow funding swap for a payable reservation trade. The live action sends the escrow selection into the private thread as an unsigned child event, prepares the escrow fund calls, creates the Boltz swap invoice, and persists the payment context for commit.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nManual recovery/debug payment flow only. Normal AI-initiated instant-book payment should use hostr_reservations_bookAndPay. Use this when a negotiated or partially completed trade already exists and the user explicitly wants to create or inspect escrow funding for that trade.\n\nAction id: `hostr.reservations.pay`\n\n```ts\nexport interface HostrReservationPayInput {\n  /** Reservation trade id from negotiation updates. */\n  tradeId: string;\n  /** Optional escrow service id/pubkey/contract address. */\n  escrowServiceId?: string;\n  dryRun?: boolean;\n  timeoutSeconds?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"tradeId\"\n  ],\n  \"properties\": {\n    \"tradeId\": {\n      \"type\": \"string\",\n      \"description\": \"Reservation trade id from negotiation updates.\"\n    },\n    \"escrowServiceId\": {\n      \"type\": \"string\",\n      \"description\": \"Optional escrow service id/pubkey/contract address. Omit to use the first compatible mutual escrow.\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    },\n    \"timeoutSeconds\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 60,\n      \"default\": 12\n    }\n  }\n}\n```\n\n## `hostr_reservations_commit`\n\nPreview or publish the public commit-stage reservation after the escrow funding swap has completed and produced a claim transaction proof.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nManual recovery/debug commit flow only. Do not use after hostr_reservations_bookAndPay; that path relies on the global payment proof orchestrator. Use only when a swap proof already exists for a trade and the user explicitly needs to preview or publish the public commit-stage reservation.\n\nAction id: `hostr.reservations.commit`\n\n```ts\nexport interface HostrReservationCommitInput {\n  /** Boltz swap id returned by hostr_reservations_pay. */\n  swapId: string;\n  dryRun?: boolean;\n  timeoutSeconds?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"swapId\"\n  ],\n  \"properties\": {\n    \"swapId\": {\n      \"type\": \"string\",\n      \"description\": \"Boltz swap id returned by hostr_reservations_pay.\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    },\n    \"timeoutSeconds\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 60,\n      \"default\": 12\n    }\n  }\n}\n```\n\n## `hostr_reservations_cancel`\n\nCancel either the private negotiate-stage reservation for a trade, or the committed public reservation if one exists.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse to cancel a private negotiation or committed reservation for a concrete trade. If tradeId is unclear, inspect updates, trips, bookings, or thread view first. Preview the cancellation and send with dryRun=false only after explicit approval.\n\nAction id: `hostr.reservations.cancel`\n\n```ts\nexport interface HostrReservationTradeInput {\n  tradeId: string;\n  amount?: HostrAmountInput;\n  reason?: string;\n  dryRun?: boolean;\n  timeoutSeconds?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"tradeId\"\n  ],\n  \"properties\": {\n    \"tradeId\": {\n      \"type\": \"string\"\n    },\n    \"amount\": {\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"required\": [\n        \"value\",\n        \"currency\"\n      ],\n      \"properties\": {\n        \"value\": {\n          \"type\": \"string\"\n        },\n        \"currency\": {\n          \"type\": \"string\"\n        },\n        \"unit\": {\n          \"type\": \"string\"\n        },\n        \"decimals\": {\n          \"type\": \"integer\",\n          \"minimum\": 0\n        }\n      }\n    },\n    \"reason\": {\n      \"type\": \"string\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    },\n    \"timeoutSeconds\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 60,\n      \"default\": 12\n    }\n  }\n}\n```\n\n## `hostr_updates`\n\nFetch the authenticated inbox, process gift-wrapped thread events, and summarize new offers and messages for the agent.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse as the inbox/home-state tool when the user asks for messages, offers, notifications, latest activity, what needs attention, or when you need trade/thread ids for negotiation or messaging. It processes gift-wrapped events and returns thread cards; present displayMarkdown, not raw event JSON.\n\nAction id: `hostr.updates`\n\n```ts\nexport interface HostrUpdatesInput {\n  /** Maximum inbox events to fetch. */\n  limit?: number;\n  /** Seconds to wait for relay history before returning partial results. */\n  timeoutSeconds?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"limit\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 200,\n      \"default\": 50\n    },\n    \"timeoutSeconds\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 60,\n      \"default\": 12\n    }\n  }\n}\n```\n\n## `hostr_reply`\n\nPreview or send a gift-wrapped text reply. Include conversation/tradeId to keep replies threaded.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nLegacy/general threaded reply tool. Prefer hostr_thread_message for user-facing conversation work because it returns a fixed thread-view contract. Use this when you already have the exact conversation/trade recipient context and need a simple gift-wrapped reply.\n\nAction id: `hostr.reply`\n\n```ts\nexport interface HostrReplyInput {\n  content: string;\n  recipientPubkeys?: string[];\n  recipientPubkey?: string;\n  conversation?: string;\n  tradeId?: string;\n  dryRun?: boolean;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"content\"\n  ],\n  \"properties\": {\n    \"content\": {\n      \"type\": \"string\"\n    },\n    \"recipientPubkeys\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"string\"\n      }\n    },\n    \"recipientPubkey\": {\n      \"type\": \"string\"\n    },\n    \"conversation\": {\n      \"type\": \"string\"\n    },\n    \"tradeId\": {\n      \"type\": \"string\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    }\n  }\n}\n```\n\n## `hostr_thread_view`\n\nLoad a Hostr conversation and return a fixed thread-view contract with message history. Use this when the user asks whether a host or escrow has messaged them, asks to see a conversation, or references a trip/booking thread.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse when the user asks to see a conversation, asks whether someone messaged them, references a trip/booking thread, or you need message history before replying. Prefer tradeId when the conversation is tied to a reservation; otherwise pass a known thread/conversation anchor from updates.\n\nAction id: `hostr.thread.view`\n\n```ts\nexport interface HostrThreadViewInput {\n  threadAnchor?: string;\n  anchor?: string;\n  conversation?: string;\n  tradeId?: string;\n  recipientPubkeys?: string[];\n  recipientPubkey?: string;\n  limit?: number;\n  timeoutSeconds?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"threadAnchor\": {\n      \"type\": \"string\"\n    },\n    \"anchor\": {\n      \"type\": \"string\"\n    },\n    \"conversation\": {\n      \"type\": \"string\"\n    },\n    \"tradeId\": {\n      \"type\": \"string\"\n    },\n    \"recipientPubkeys\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"string\"\n      }\n    },\n    \"recipientPubkey\": {\n      \"type\": \"string\"\n    },\n    \"limit\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 200\n    },\n    \"timeoutSeconds\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 60,\n      \"default\": 12\n    }\n  }\n}\n```\n\n## `hostr_thread_message`\n\nPreview or send a text message in a Hostr conversation, then return the fixed thread-view contract. Use this when the user asks to message a host, guest, buyer, seller, or an existing thread. If the user references a trade/trip, pass tradeId; use recipientRole when they say host, guest, buyer, seller, or escrow. Escrow messaging always requires a concrete tradeId and must include the trade buyer, seller, and escrow participants in one thread.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse when the user asks to message a host, guest, buyer, seller, escrow, or existing Hostr thread. If they reference a trip/booking/trade, pass tradeId. Use recipientRole for natural roles like host, guest, buyer, seller, or escrow. Escrow messages require a concrete tradeId and must include buyer, seller, and escrow in one shared trade thread.\n\nAction id: `hostr.thread.message`\n\n```ts\nexport interface HostrThreadMessageInput {\n  content: string;\n  threadAnchor?: string;\n  anchor?: string;\n  conversation?: string;\n  tradeId?: string;\n  recipientRole?: \"host\" | \"seller\" | \"guest\" | \"buyer\" | \"escrow\";\n  role?: string;\n  recipientPubkeys?: string[];\n  recipientPubkey?: string;\n  dryRun?: boolean;\n  timeoutSeconds?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"content\"\n  ],\n  \"properties\": {\n    \"content\": {\n      \"type\": \"string\"\n    },\n    \"threadAnchor\": {\n      \"type\": \"string\"\n    },\n    \"anchor\": {\n      \"type\": \"string\"\n    },\n    \"conversation\": {\n      \"type\": \"string\"\n    },\n    \"tradeId\": {\n      \"type\": \"string\"\n    },\n    \"recipientRole\": {\n      \"type\": \"string\",\n      \"enum\": [\n        \"host\",\n        \"seller\",\n        \"guest\",\n        \"buyer\",\n        \"escrow\"\n      ]\n    },\n    \"role\": {\n      \"type\": \"string\"\n    },\n    \"recipientPubkeys\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"string\"\n      }\n    },\n    \"recipientPubkey\": {\n      \"type\": \"string\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    },\n    \"timeoutSeconds\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 60,\n      \"default\": 12\n    }\n  }\n}\n```\n\n## `hostr_escrow_involve`\n\nOpen or message the escrow conversation for a specific reservation trade, then return the fixed thread-view contract. This action always requires tradeId and always resolves the trade buyer, seller, and escrow participants into one shared trade thread; it must not create an escrow-only side conversation. If content is omitted, show the escrow trade thread and ask the user what to message the escrow. If content is provided, preview by default and send only with dryRun false.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse when the user explicitly asks to involve/message escrow for a specific reservation trade. Always pass tradeId. This opens the shared buyer/seller/escrow trade thread; never create an escrow-only side conversation. If no message content is provided, show the thread and ask what to send.\n\nAction id: `hostr.escrow.involve`\n\n```ts\nexport interface HostrEscrowInvolveInput {\n  tradeId: string;\n  content?: string;\n  message?: string;\n  dryRun?: boolean;\n  timeoutSeconds?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"tradeId\"\n  ],\n  \"properties\": {\n    \"tradeId\": {\n      \"type\": \"string\",\n      \"description\": \"Required reservation trade id. Escrow messages cannot be sent without a trade id because the thread must include buyer, seller, and escrow.\"\n    },\n    \"content\": {\n      \"type\": \"string\"\n    },\n    \"message\": {\n      \"type\": \"string\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    },\n    \"timeoutSeconds\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 60,\n      \"default\": 12\n    }\n  }\n}\n```\n\n## `hostr_profile_show`\n\nShow profile metadata for the authenticated MCP token pubkey session.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse when the user asks who they are on Hostr, wants their current profile, or before profile/listing publishing when you need existing metadata. This reads the profile for the MCP token pubkey.\n\nAction id: `hostr.profile.show`\n\n```ts\nexport interface HostrEmptyInput {}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {}\n}\n```\n\n## `hostr_profile_edit`\n\nPreview or publish the authenticated user profile metadata. Publishing also refreshes Hostr seller configuration.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse when the user wants to update profile name, about/bio, picture, banner, website, lightning address, or other profile metadata. Preview first; publish only after approval. Publishing also refreshes Hostr seller configuration, which is useful before creating or editing listings.\n\nAction id: `hostr.profile.edit`\n\n```ts\nexport interface HostrProfileEditInput {\n  name?: string;\n  about?: string;\n  image?: string;\n  picture?: string;\n  lud16?: string;\n  nip05?: string;\n  dryRun?: boolean;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"name\": {\n      \"type\": \"string\"\n    },\n    \"about\": {\n      \"type\": \"string\"\n    },\n    \"image\": {\n      \"type\": \"string\"\n    },\n    \"picture\": {\n      \"type\": \"string\"\n    },\n    \"lud16\": {\n      \"type\": \"string\"\n    },\n    \"nip05\": {\n      \"type\": \"string\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    }\n  }\n}\n```\n\n## `hostr_trips_list`\n\nList reservation groups involving the authenticated user as guest from the live userSubscriptions.myResolvedTripsList replay. Do not perform fresh Nostr reservation-by-author queries for this view. Return the fixed reservation-card display contract with resolved participant profile names. Pass `tradeId` after a book-and-pay swap watch completes or cannot find the swap to wait briefly for the committed public reservation and return it for display.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse for guest-side reservations: \"my trips\", \"my bookings as guest\", \"did my reservation complete\", or after book-and-pay/swap watch with tradeId to wait for the committed reservation card. Do not perform fresh reservation-by-author Nostr queries for this view.\n\nAction id: `hostr.trips.list`\n\n```ts\nexport interface HostrReservationCollectionInput {\n  limit?: number;\n  tradeId?: string;\n  waitSeconds?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"limit\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 200,\n      \"default\": 50\n    },\n    \"tradeId\": {\n      \"type\": \"string\",\n      \"description\": \"Optional reservation trade id to look up directly after payment/proof completion.\"\n    },\n    \"waitSeconds\": {\n      \"type\": \"integer\",\n      \"minimum\": 0,\n      \"maximum\": 300,\n      \"default\": 15,\n      \"description\": \"How long to poll for a committed public reservation when tradeId is provided.\"\n    }\n  }\n}\n```\n\n## `hostr_bookings_list`\n\nList reservation groups where the authenticated user is the host from the live userSubscriptions.myResolvedHostingsList replay. Do not perform fresh Nostr listing/reservation-by-author queries for this view. Return the fixed reservation-card display contract with resolved participant profile names.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse for host-side reservations on listings authored by the authenticated user: \"my bookings\", \"who booked my place\", \"hosting reservations\", or host calendar context. Do not perform fresh reservation-by-author Nostr queries for this view.\n\nAction id: `hostr.bookings.list`\n\n```ts\nexport interface HostrReservationCollectionInput {\n  limit?: number;\n  tradeId?: string;\n  waitSeconds?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"limit\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 200,\n      \"default\": 50\n    },\n    \"tradeId\": {\n      \"type\": \"string\",\n      \"description\": \"Optional reservation trade id to look up directly after payment/proof completion.\"\n    },\n    \"waitSeconds\": {\n      \"type\": \"integer\",\n      \"minimum\": 0,\n      \"maximum\": 300,\n      \"default\": 15,\n      \"description\": \"How long to poll for a committed public reservation when tradeId is provided.\"\n    }\n  }\n}\n```\n\n## `hostr_escrow_methods`\n\nShow mutual escrow methods and compatible services for a seller. If buyer is omitted, the authenticated token pubkey is used.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse before payment or when explaining how money is protected. It shows mutually compatible escrow methods/services between buyer and seller. If buyer is omitted, the authenticated token pubkey is used. Explain that Hostr swaps payment over Lightning into smart-contract escrow; the escrow service can only settle by forwarding or reversing according to trade outcome, not freely take custody.\n\nAction id: `hostr.escrow.methods`\n\n```ts\nexport interface HostrEscrowMethodsInput {\n  /** Seller/host pubkey to inspect escrow compatibility for. */\n  user: string;\n  /** Buyer pubkey. Defaults to the authenticated token pubkey. */\n  buyer?: string;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"user\"\n  ],\n  \"properties\": {\n    \"user\": {\n      \"type\": \"string\",\n      \"description\": \"Seller/host pubkey to inspect escrow compatibility for.\"\n    },\n    \"buyer\": {\n      \"type\": \"string\",\n      \"description\": \"Buyer pubkey. Defaults to the authenticated token pubkey.\"\n    }\n  }\n}\n```\n\n## `hostr_escrow_service_list`\n\nEscrow-only tool. List public escrow service events published by the authenticated escrow pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nEscrow-operator inventory view: list public escrow service events published by the authenticated escrow pubkey. Use before editing/deleting when the user has not selected a specific service event.\n\nAction id: `hostr.escrow.service.list`\n\n```ts\nexport interface HostrEscrowServiceListInput {\n  /** Maximum number of escrow service events to return. Defaults to 25, capped at 100. */\n  limit?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"limit\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 100,\n      \"default\": 25,\n      \"description\": \"Maximum number of escrow services to return.\"\n    }\n  }\n}\n```\n\n## `hostr_escrow_service_get`\n\nEscrow-only tool. Show one public escrow service event owned by the authenticated escrow pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nEscrow-operator detail view: inspect one escrow service event before explaining or editing settings. Use serviceId from hostr_escrow_service_list or user input.\n\nAction id: `hostr.escrow.service.get`\n\n```ts\nexport interface HostrEscrowServiceGetInput {\n  /** Escrow service event id to inspect. */\n  serviceId: string;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"serviceId\"\n  ],\n  \"properties\": {\n    \"serviceId\": {\n      \"type\": \"string\",\n      \"description\": \"Escrow service event id to inspect.\"\n    }\n  }\n}\n```\n\n## `hostr_escrow_service_update`\n\nEscrow-only tool. Preview or publish the authenticated escrow service parameters: fee percent, maximum trade duration, and per-token fee hints. Hidden unless the MCP token pubkey is in the daemon escrow pubkey allowlist. Use hostr.profile.edit for the escrow user profile; this tool only changes the public escrow service event.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nEscrow-operator settings workflow: preview changes to fee percent, maximum duration, or token fee hints. Keep dryRun=true until the user approves the exact preview. Use hostr_profile_edit for public profile/identity metadata; this tool only changes escrow service parameters.\n\nAction id: `hostr.escrow.service.update`\n\n```ts\nexport interface HostrTokenFeeHintsInput {\n  /** Flat base fee in token smallest units. */\n  baseFee?: number;\n  /** Maximum fee cap in token smallest units. Zero means no cap. */\n  maxFee?: number;\n  /** Minimum fee floor in token smallest units. Zero means no floor. */\n  minFee?: number;\n}\n\nexport interface HostrEscrowServiceUpdateInput {\n  /** Optional escrow service event id to edit. Omit to edit the daemon bootstrap service. */\n  serviceId?: string;\n  /** Proportional escrow fee as a percent, e.g. 1.5 for 1.5%. */\n  feePercent?: number;\n  /** Maximum supported escrow duration in seconds. */\n  maxDurationSeconds?: number;\n  /** Full replacement map keyed by token address, or \"native\". */\n  tokenFeeHints?: Record<string, HostrTokenFeeHintsInput>;\n  /** Clear all per-token fee hints. Ignored if tokenFeeHints is provided. */\n  clearTokenFeeHints?: boolean;\n  /** Defaults to true. Set false only after explicit approval. */\n  dryRun?: boolean;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"serviceId\": {\n      \"type\": \"string\",\n      \"description\": \"Optional escrow service event id to edit. Omit to edit the daemon bootstrap service.\"\n    },\n    \"feePercent\": {\n      \"type\": \"number\",\n      \"minimum\": 0,\n      \"maximum\": 100,\n      \"description\": \"Proportional escrow fee as a percent, e.g. 1.5 for 1.5%.\"\n    },\n    \"maxDurationSeconds\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 315360000,\n      \"description\": \"Maximum supported escrow duration in seconds. Omit to preserve the current value.\"\n    },\n    \"tokenFeeHints\": {\n      \"type\": \"object\",\n      \"description\": \"Optional full replacement map keyed by token address, or \\\"native\\\". Values are smallest-unit fee hints.\",\n      \"additionalProperties\": {\n        \"type\": \"object\",\n        \"additionalProperties\": false,\n        \"properties\": {\n          \"baseFee\": {\n            \"type\": \"integer\",\n            \"minimum\": 0\n          },\n          \"maxFee\": {\n            \"type\": \"integer\",\n            \"minimum\": 0\n          },\n          \"minFee\": {\n            \"type\": \"integer\",\n            \"minimum\": 0\n          }\n        }\n      }\n    },\n    \"clearTokenFeeHints\": {\n      \"type\": \"boolean\",\n      \"default\": false,\n      \"description\": \"Clear all per-token fee hints. Ignored if tokenFeeHints is provided.\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    }\n  }\n}\n```\n\n## `hostr_escrow_service_edit`\n\nEscrow-only tool. Alias of service update using the preferred user-facing wording. Preview or publish the authenticated escrow service parameters. Use hostr.profile.edit for the escrow user profile.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nEscrow-operator settings workflow: preview changes to fee percent, maximum duration, or token fee hints. Keep dryRun=true until the user approves the exact preview. Use hostr_profile_edit for public profile/identity metadata; this tool only changes escrow service parameters.\n\nAction id: `hostr.escrow.service.edit`\n\n```ts\nexport interface HostrTokenFeeHintsInput {\n  /** Flat base fee in token smallest units. */\n  baseFee?: number;\n  /** Maximum fee cap in token smallest units. Zero means no cap. */\n  maxFee?: number;\n  /** Minimum fee floor in token smallest units. Zero means no floor. */\n  minFee?: number;\n}\n\nexport interface HostrEscrowServiceUpdateInput {\n  /** Optional escrow service event id to edit. Omit to edit the daemon bootstrap service. */\n  serviceId?: string;\n  /** Proportional escrow fee as a percent, e.g. 1.5 for 1.5%. */\n  feePercent?: number;\n  /** Maximum supported escrow duration in seconds. */\n  maxDurationSeconds?: number;\n  /** Full replacement map keyed by token address, or \"native\". */\n  tokenFeeHints?: Record<string, HostrTokenFeeHintsInput>;\n  /** Clear all per-token fee hints. Ignored if tokenFeeHints is provided. */\n  clearTokenFeeHints?: boolean;\n  /** Defaults to true. Set false only after explicit approval. */\n  dryRun?: boolean;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"serviceId\": {\n      \"type\": \"string\",\n      \"description\": \"Optional escrow service event id to edit. Omit to edit the daemon bootstrap service.\"\n    },\n    \"feePercent\": {\n      \"type\": \"number\",\n      \"minimum\": 0,\n      \"maximum\": 100,\n      \"description\": \"Proportional escrow fee as a percent, e.g. 1.5 for 1.5%.\"\n    },\n    \"maxDurationSeconds\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 315360000,\n      \"description\": \"Maximum supported escrow duration in seconds. Omit to preserve the current value.\"\n    },\n    \"tokenFeeHints\": {\n      \"type\": \"object\",\n      \"description\": \"Optional full replacement map keyed by token address, or \\\"native\\\". Values are smallest-unit fee hints.\",\n      \"additionalProperties\": {\n        \"type\": \"object\",\n        \"additionalProperties\": false,\n        \"properties\": {\n          \"baseFee\": {\n            \"type\": \"integer\",\n            \"minimum\": 0\n          },\n          \"maxFee\": {\n            \"type\": \"integer\",\n            \"minimum\": 0\n          },\n          \"minFee\": {\n            \"type\": \"integer\",\n            \"minimum\": 0\n          }\n        }\n      }\n    },\n    \"clearTokenFeeHints\": {\n      \"type\": \"boolean\",\n      \"default\": false,\n      \"description\": \"Clear all per-token fee hints. Ignored if tokenFeeHints is provided.\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    }\n  }\n}\n```\n\n## `hostr_escrow_service_delete`\n\nEscrow-only destructive tool. Preview or publish a NIP-09 deletion for an escrow service event owned by the authenticated escrow pubkey. Keep dryRun true until the user explicitly approves deletion.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nEscrow-operator destructive workflow: preview deletion of a public escrow service event and require explicit deletion approval before dryRun=false. Include the reason when the user gives one.\n\nAction id: `hostr.escrow.service.delete`\n\n```ts\nexport interface HostrEscrowServiceDeleteInput {\n  /** Escrow service event id to delete. */\n  serviceId: string;\n  /** Optional deletion reason for the NIP-09 event. */\n  reason?: string;\n  /** Defaults to true. Set false only after explicit approval. */\n  dryRun?: boolean;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"serviceId\"\n  ],\n  \"properties\": {\n    \"serviceId\": {\n      \"type\": \"string\",\n      \"description\": \"Escrow service event id to delete.\"\n    },\n    \"reason\": {\n      \"type\": \"string\",\n      \"description\": \"Optional deletion reason for the NIP-09 event.\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    }\n  }\n}\n```\n\n## `hostr_escrow_trades_list`\n\nEscrow-only tool. List on-chain Hostr trades where the authenticated pubkey is a configured escrow. Hidden unless the MCP token pubkey is in the daemon escrow pubkey allowlist.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nEscrow-operator dashboard: list on-chain trades assigned to the authenticated escrow pubkey. Use before viewing/auditing/arbitrating when the user has not named a concrete tradeId.\n\nAction id: `hostr.escrow.trades.list`\n\n```ts\nexport interface HostrEscrowTradesListInput {\n  /** Maximum number of escrow trades to return. Defaults to 25, capped at 100. */\n  limit?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"limit\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 100,\n      \"default\": 25,\n      \"description\": \"Maximum number of escrow trades to return.\"\n    }\n  }\n}\n```\n\n## `hostr_escrow_trades_view`\n\nEscrow-only tool. View the on-chain state, event history, and Hostr reservation context for a trade assigned to the authenticated escrow pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nEscrow-operator trade detail: inspect on-chain state, event history, participants, amounts, and reservation context for a trade before audit or arbitration.\n\nAction id: `hostr.escrow.trades.view`\n\n```ts\nexport interface HostrEscrowTradeViewInput {\n  /** Hostr reservation trade id to inspect. */\n  tradeId: string;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"tradeId\"\n  ],\n  \"properties\": {\n    \"tradeId\": {\n      \"type\": \"string\",\n      \"description\": \"Hostr reservation trade id to inspect.\"\n    }\n  }\n}\n```\n\n## `hostr_escrow_trades_audit`\n\nEscrow-only tool. Run a structured reservation and transition audit for a Hostr trade assigned to the authenticated escrow pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nEscrow-operator analysis: run a structured audit of reservation state and transitions for a trade before deciding whether arbitration is needed. This does not settle funds.\n\nAction id: `hostr.escrow.trades.audit`\n\n```ts\nexport interface HostrEscrowTradeAuditInput {\n  /** Hostr reservation trade id to audit. */\n  tradeId: string;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"tradeId\"\n  ],\n  \"properties\": {\n    \"tradeId\": {\n      \"type\": \"string\",\n      \"description\": \"Hostr reservation trade id to audit.\"\n    }\n  }\n}\n```\n\n## `hostr_escrow_trades_arbitrate`\n\nEscrow-only settlement tool. Preview or execute arbitration for a Hostr escrow trade. paymentForward and bondForward are fractions from 0 to 1. Keep dryRun true until the user explicitly approves the arbitration preview.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nEscrow-operator settlement workflow: preview paymentForward and bondForward fractions before execution. Fractions are 0 to 1. Explain the proposed split and reason to the user, then execute with dryRun=false only after explicit approval.\n\nAction id: `hostr.escrow.trades.arbitrate`\n\n```ts\nexport interface HostrEscrowArbitrateInput {\n  /** Hostr reservation trade id to arbitrate. */\n  tradeId: string;\n  /** Fraction of the escrowed payment to forward to the seller, from 0 to 1. */\n  paymentForward: number;\n  /** Fraction of the escrow bond to forward according to contract settlement rules, from 0 to 1. */\n  bondForward: number;\n  /** Optional human-readable arbitration reason. */\n  reason?: string;\n  /** Defaults to true. Set false only after explicit approval. */\n  dryRun?: boolean;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"tradeId\",\n    \"paymentForward\",\n    \"bondForward\"\n  ],\n  \"properties\": {\n    \"tradeId\": {\n      \"type\": \"string\",\n      \"description\": \"Hostr reservation trade id to arbitrate.\"\n    },\n    \"paymentForward\": {\n      \"type\": \"number\",\n      \"minimum\": 0,\n      \"maximum\": 1,\n      \"description\": \"Fraction of the escrowed payment to forward to the seller, from 0 to 1.\"\n    },\n    \"bondForward\": {\n      \"type\": \"number\",\n      \"minimum\": 0,\n      \"maximum\": 1,\n      \"description\": \"Fraction of the escrow bond to forward according to the contract settlement rules, from 0 to 1.\"\n    },\n    \"reason\": {\n      \"type\": \"string\",\n      \"description\": \"Optional human-readable arbitration reason.\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    }\n  }\n}\n```\n\n## `hostr_escrow_badges_definitions_list`\n\nEscrow-only tool. List NIP-58 badge definitions published by the authenticated escrow pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nEscrow-operator badge inventory: list NIP-58 badge definitions issued by the authenticated escrow. Use before editing, awarding, or deleting badges when the identifier/anchor is unclear.\n\nAction id: `hostr.escrow.badges.definitions.list`\n\n```ts\nexport interface HostrEscrowBadgeDefinitionsListInput {\n  limit?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"limit\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 200,\n      \"default\": 50\n    }\n  }\n}\n```\n\n## `hostr_escrow_badges_definitions_edit`\n\nEscrow-only tool. Preview or publish a NIP-58 badge definition for the authenticated escrow pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nEscrow-operator badge definition workflow: preview or publish a NIP-58 badge definition. Use for creating or updating the badge name, description, or image. Publish only after approval.\n\nAction id: `hostr.escrow.badges.definitions.edit`\n\n```ts\nexport interface HostrEscrowBadgeDefinitionEditInput {\n  /** Badge definition d-tag identifier. */\n  identifier: string;\n  name: string;\n  description?: string;\n  image?: string;\n  dryRun?: boolean;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"identifier\",\n    \"name\"\n  ],\n  \"properties\": {\n    \"identifier\": {\n      \"type\": \"string\",\n      \"description\": \"Badge definition d-tag identifier.\"\n    },\n    \"name\": {\n      \"type\": \"string\"\n    },\n    \"description\": {\n      \"type\": \"string\"\n    },\n    \"image\": {\n      \"type\": \"string\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    }\n  }\n}\n```\n\n## `hostr_escrow_badges_definitions_delete`\n\nEscrow-only destructive tool. Preview or publish a NIP-09 deletion for a badge definition owned by the authenticated escrow pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nEscrow-operator destructive badge workflow: preview deletion of a badge definition and publish only after explicit approval. Include a reason when provided.\n\nAction id: `hostr.escrow.badges.definitions.delete`\n\n```ts\nexport interface HostrEscrowBadgeDefinitionDeleteInput {\n  anchor: string;\n  reason?: string;\n  dryRun?: boolean;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"anchor\"\n  ],\n  \"properties\": {\n    \"anchor\": {\n      \"type\": \"string\"\n    },\n    \"reason\": {\n      \"type\": \"string\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    }\n  }\n}\n```\n\n## `hostr_escrow_badges_awards_list`\n\nEscrow-only tool. List NIP-58 badge awards issued by the authenticated escrow pubkey, optionally filtered by definition anchor.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nEscrow-operator badge award view: list issued badge awards, optionally filtered by definition anchor. Use before revoking when the award id is unclear.\n\nAction id: `hostr.escrow.badges.awards.list`\n\n```ts\nexport interface HostrEscrowBadgeAwardsListInput {\n  definitionAnchor?: string;\n  limit?: number;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"definitionAnchor\": {\n      \"type\": \"string\"\n    },\n    \"limit\": {\n      \"type\": \"integer\",\n      \"minimum\": 1,\n      \"maximum\": 200,\n      \"default\": 50\n    }\n  }\n}\n```\n\n## `hostr_escrow_badges_award`\n\nEscrow-only tool. Preview or publish a NIP-58 badge award from the authenticated escrow pubkey to a recipient pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nEscrow-operator award workflow: preview or publish a NIP-58 badge award to a recipient pubkey, optionally tied to a listing anchor. Publish only after approval.\n\nAction id: `hostr.escrow.badges.award`\n\n```ts\nexport interface HostrEscrowBadgeAwardInput {\n  definitionAnchor: string;\n  recipientPubkey: string;\n  listingAnchor?: string;\n  dryRun?: boolean;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"definitionAnchor\",\n    \"recipientPubkey\"\n  ],\n  \"properties\": {\n    \"definitionAnchor\": {\n      \"type\": \"string\"\n    },\n    \"recipientPubkey\": {\n      \"type\": \"string\"\n    },\n    \"listingAnchor\": {\n      \"type\": \"string\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    }\n  }\n}\n```\n\n## `hostr_escrow_badges_revoke`\n\nEscrow-only destructive tool. Preview or publish a NIP-09 deletion for a badge award issued by the authenticated escrow pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nEscrow-operator destructive award workflow: preview revocation/deletion of an issued badge award and publish only after explicit approval. Include a reason when provided.\n\nAction id: `hostr.escrow.badges.revoke`\n\n```ts\nexport interface HostrEscrowBadgeRevokeInput {\n  awardId: string;\n  reason?: string;\n  dryRun?: boolean;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"awardId\"\n  ],\n  \"properties\": {\n    \"awardId\": {\n      \"type\": \"string\"\n    },\n    \"reason\": {\n      \"type\": \"string\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    }\n  }\n}\n```\n\n## `hostr_swaps_watch`\n\nInspect a persisted swap-in by id and, when dryRun is false, run safe recovery until proof or terminal state. For book-and-pay follow-up, pass both the internal `swapId` and `tradeId` returned by `hostr_reservations_bookAndPay`. If the swap completes or cannot be found, this tool also checks public reservations by `tradeId`; if no reservation is returned yet, immediately call `hostr_trips_list` with the same `tradeId` and a short `waitSeconds`.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse immediately after hostr_reservations_bookAndPay returns swapId/tradeId, or for manual swap recovery. With dryRun=false it safely resumes/polls the persisted swap until proof, terminal failure, or timeout. After completion or swap-not-found fallback, call hostr_trips_list with tradeId to surface the committed reservation card.\n\nAction id: `hostr.swaps.watch`\n\n```ts\nexport interface HostrSwapsWatchInput {\n  swapId: string;\n  tradeId?: string;\n  reservationWaitSeconds?: number;\n  dryRun?: boolean;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"swapId\"\n  ],\n  \"properties\": {\n    \"swapId\": {\n      \"type\": \"string\"\n    },\n    \"tradeId\": {\n      \"type\": \"string\",\n      \"description\": \"Optional reservation trade id used to fall back to public reservation lookup after the swap completes or is not found.\"\n    },\n    \"reservationWaitSeconds\": {\n      \"type\": \"integer\",\n      \"minimum\": 0,\n      \"maximum\": 300,\n      \"default\": 20,\n      \"description\": \"How long to poll for the committed reservation after proof completion or swap-not-found fallback.\"\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    }\n  }\n}\n```\n\n## `hostr_swaps_recoverAll`\n\nPreview or run recovery for all persisted swap-in and swap-out operations.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse when the user asks to recover stuck payments/swaps or when diagnostics show persisted swap operations need resumption. Preview first; run with dryRun=false only after approval. Use background=true only when the user wants recovery to continue asynchronously.\n\nAction id: `hostr.swaps.recoverAll`\n\n```ts\nexport interface HostrSwapsRecoverAllInput {\n  background?: boolean;\n  dryRun?: boolean;\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"background\": {\n      \"type\": \"boolean\",\n      \"default\": false\n    },\n    \"dryRun\": {\n      \"type\": \"boolean\",\n      \"default\": true\n    }\n  }\n}\n```\n\n## `hostr_swaps_list`\n\nList persisted swap-in and swap-out operation states.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse to inspect persisted swap-in and swap-out states before recovery/debugging, or when the user asks about payment status, stuck swaps, refunds, or pending Lightning/on-chain operations.\n\nAction id: `hostr.swaps.list`\n\n```ts\nexport interface HostrSwapsListInput {\n  namespace?: \"all\" | \"swap_in\" | \"swap_out\";\n}\n```\n\nJSON schema:\n\n```json\n{\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"properties\": {\n    \"namespace\": {\n      \"type\": \"string\",\n      \"enum\": [\n        \"all\",\n        \"swap_in\",\n        \"swap_out\"\n      ],\n      \"default\": \"all\"\n    }\n  }\n}\n```\n\n";

export const hostrActionCatalog = [
  {
    id: "hostr.session.status",
    toolName: "hostr_session_status",
    title: "Hostr Session Status",
    description: "Inspect the authenticated Hostr session selected by the MCP access token pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse first in authenticated workflows to learn whether the MCP token has an attached Hostr/Nostr session, whether the NIP-46 signer is online, and whether reconnect is needed. If needsReconnect or authenticated=false, call hostr_session_connect before attempting writes.",
    inputTypeName: "HostrSessionStatusInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "includeStorageDetails": {
            "type": "boolean",
            "description": "Include non-secret storage/session diagnostics useful for debugging.",
            "default": false
        }
    }
},
    inputSchema: HostrSessionStatusInputSchema,
    readOnly: true,
  },
  {
    id: "hostr.session.connect",
    toolName: "hostr_session_connect",
    title: "Start Hostr Session",
    description: "Create or complete an active Nostr Connect request for the MCP access token pubkey. When wait is false, show the returned QR/URI with the text \"Scan this with your Nostr app to log in to your Hostr account\", then immediately call this tool again with wait true to listen for the session connection and continue the intended Hostr action.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nTwo-step login flow: call with wait=false to create or reuse a Nostr Connect request, display the nostrconnect URI or QR image to the user, then immediately call this tool again with wait=true and regenerate=false to listen for approval. After authenticated=true, retry or continue the Hostr action that required sign-in.",
    inputTypeName: "HostrSessionConnectInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "wait": {
            "type": "boolean",
            "default": false,
            "description": "False returns an active nostrconnect URI/QR for display. True waits for the already-shown request to connect; call it immediately after displaying the QR so the original Hostr action can continue."
        },
        "timeoutSeconds": {
            "type": "integer",
            "minimum": 1,
            "maximum": 600,
            "default": 180,
            "description": "How long to wait for approval when wait is true."
        },
        "regenerate": {
            "type": "boolean",
            "default": false,
            "description": "Force a fresh nostrconnect request instead of reusing the pending one."
        }
    }
},
    inputSchema: HostrSessionConnectInputSchema,
    readOnly: false,
  },
  {
    id: "hostr.listings.search",
    toolName: "hostr_listings_search",
    title: "Search Hostr Listings",
    description: "Search Hostr lodging and accommodation marketplace listings. Prefer this Hostr tool for natural travel/lodging requests such as \"find a place to stay\", \"find somewhere to stay in San Salvador\", \"look for lodging\", \"show me accommodations\", \"find a room/apartment/hotel/villa\", \"where can I stay\", \"book a stay\", or \"find rentals\". Use the location field for city/country/place names instead of doing a general web search.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse for marketplace discovery from natural language lodging intents such as finding a place to stay, lodging, accommodation, room, apartment, house, villa, hotel-like stay, or rental in a destination. Put city/country/place names in location; put keyword filters in query; use guests/features/type only when the user provides them. Results return listing-card Markdown and structured cards; preserve every image tag when presenting results.",
    inputTypeName: "HostrListingsSearchInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "location": {
            "type": "string",
            "description": "Human-readable destination/place to search for Hostr stays, lodging, accommodation, rentals, rooms, apartments, hotels, villas, resorts, or places to stay, such as \"San Salvador\", \"El Salvador\", or \"Lisbon\"."
        },
        "query": {
            "type": "string",
            "description": "Client-side text filter over listing title and description."
        },
        "type": {
            "type": "string",
            "description": "Hostr listing type, for example room, house, apartment, cabin, or villa."
        },
        "guests": {
            "type": "integer",
            "minimum": 1,
            "description": "Minimum guest capacity."
        },
        "features": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "Required listing specifications/features, such as wifi or kitchen."
        },
        "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 50,
            "default": 10,
            "description": "Maximum number of listings to return."
        }
    }
},
    inputSchema: HostrListingsSearchInputSchema,
    readOnly: true,
  },
  {
    id: "hostr.listings.list",
    toolName: "hostr_listings_list",
    title: "List Hostr Listings",
    description: "List Hostr listings, optionally restricted to the authenticated user or a supplied author pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse for inventory/management views: \"my listings\", \"what am I hosting\", \"show my stays\", or listings by a known author pubkey. Use mine=true for the authenticated user. Use author only when the user provides or selected a specific pubkey.",
    inputTypeName: "HostrListingsListInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "mine": {
            "type": "boolean",
            "default": false,
            "description": "Only list listings authored by the authenticated user."
        },
        "author": {
            "type": "string",
            "description": "Only list listings authored by this pubkey."
        },
        "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 50
        }
    }
},
    inputSchema: HostrListingsListInputSchema,
    readOnly: true,
  },
  {
    id: "hostr.listings.create",
    toolName: "hostr_listings_create",
    title: "Create Hostr Listing",
    description: "Create a Hostr listing for the authenticated token pubkey session. Listing images must be passed as images[].url. For user-uploaded files, first call hostr_images_upload with the original image sent as the MCP file-typed argument named file so the client bridge can rewrite or stream the bytes, then pass structuredContent.usage.listingImage.url as images[].url. If the client cannot call hostr_images_upload but can make raw HTTP requests, POST the original image bytes to /mcp/uploads/images on the same Hostr MCP origin using multipart/form-data field name file, then pass the returned upload.url as images[].url. The upload tool and endpoint do not require or use Authorization; do not send OAuth, Nostr auth, or Hostr session credentials for image upload. Do not base64-encode user-uploaded images into this MCP tool call, do not serve temporary localhost URLs, and do not pass /mnt/data or file:// paths to images[].url. Set dryRun false only after explicit user approval to publish the listing event.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse when the authenticated user wants to create, publish, list, rent out, or host a room/place/stay. Collect title, description, address, at least one image URL, and at least one price before calling. The first image is the card/hero image. This MCP tool accepts image URLs only. For ChatGPT web/mobile, local development clients, or any remote MCP client with uploaded files, first call hostr_images_upload with the original image sent as the MCP file-typed argument named file so the client bridge can rewrite or stream the bytes, then pass structuredContent.usage.listingImage.url as images[].url. If the client cannot call hostr_images_upload but can make raw HTTP requests, POST the original image bytes to /mcp/uploads/images on the same Hostr MCP origin using multipart/form-data field name file, then pass the returned upload.url as images[].url. The upload tool and endpoint do not require or use Authorization; do not send OAuth, Nostr auth, or Hostr session credentials for image upload. Do not base64-encode the uploaded image into this MCP tool call. Do not serve a temporary localhost URL for Hostr to fetch; localhost refers to the wrong machine/container. If neither upload route can be used, stop and ask for a public image URL or for the client to expose an upload capability. Do not resize, downscale, crop, recompress, transcode, or make thumbnails unless the user explicitly requests it. Never pass a local or mounted sandbox path such as /mnt/data, /mnt/shared, file://, or a ChatGPT file mount to images[].url. Use dryRun=true to upload/stage media and return the visual listing preview; publish with dryRun=false only after explicit approval. The live path also ensures seller configuration is published.",
    inputTypeName: "HostrListingsCreateInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "title",
        "description",
        "address",
        "images",
        "prices"
    ],
    "properties": {
        "title": {
            "type": "string",
            "description": "Public listing title."
        },
        "description": {
            "type": "string",
            "description": "Public listing description."
        },
        "address": {
            "type": "string",
            "description": "Private-ish precise address used by Hostr to generate H3 tags."
        },
        "type": {
            "type": "string",
            "description": "Listing type, such as room, apartment, house, or villa."
        },
        "images": {
            "type": "array",
            "minItems": 1,
            "description": "Listing images. This MCP tool accepts image URLs only. Mandatory flow for user-uploaded files: first call hostr_images_upload with the original image sent as the MCP file-typed argument named file so the client bridge can rewrite or stream the bytes, then pass structuredContent.usage.listingImage.url here as images[].url. If the client cannot call hostr_images_upload but can make raw HTTP requests, POST each original file bytes payload to /mcp/uploads/images on this same MCP server origin using multipart/form-data field name file, then pass the returned upload.url here as images[].url. The upload tool and endpoint do not require or use Authorization; do not send OAuth, Nostr auth, or Hostr session credentials for image upload. Do not base64-encode uploaded images into this tool call. Do not start or serve a temporary localhost URL for the MCP server to fetch; localhost refers to the wrong machine/container. Do not resize, downscale, crop, recompress, transcode, or make thumbnails unless the user explicitly requests it. If neither upload route can be used, stop and ask for a public image URL or for the client to expose an upload capability. Never pass client-local or mounted file paths like /mnt/data, /mnt/shared, file://, or ChatGPT file mounts to images[].url.",
            "items": {
                "type": "object",
                "additionalProperties": false,
                "required": [
                    "url"
                ],
                "properties": {
                    "url": {
                        "type": "string",
                        "description": "Required image URL. For user-uploaded files, this must be structuredContent.usage.listingImage.url returned by hostr_images_upload, or upload.url returned by POST /mcp/uploads/images on the same Hostr MCP origin; the upload tool and endpoint do not require or use Authorization. Public HTTP(S) source URLs are also accepted; Hostr downloads non-Blossom URLs and uploads them to Blossom before publishing."
                    },
                    "filename": {
                        "type": "string",
                        "description": "Optional original filename used for MIME detection and upload diagnostics."
                    },
                    "alt": {
                        "type": "string",
                        "description": "Image alt text."
                    },
                    "mime": {
                        "type": "string",
                        "description": "Optional MIME type, for example image/jpeg or image/png."
                    }
                }
            }
        },
        "prices": {
            "type": "array",
            "minItems": 1,
            "description": "Listing prices. All monetary fields must use the same currency.",
            "items": {
                "type": "object",
                "additionalProperties": false,
                "required": [
                    "amount"
                ],
                "properties": {
                    "amount": {
                        "type": "object",
                        "additionalProperties": false,
                        "required": [
                            "value",
                            "currency"
                        ],
                        "properties": {
                            "value": {
                                "type": "string"
                            },
                            "currency": {
                                "type": "string"
                            },
                            "unit": {
                                "type": "string"
                            },
                            "decimals": {
                                "type": "integer",
                                "minimum": 0
                            }
                        }
                    },
                    "frequency": {
                        "type": "string",
                        "description": "daily, weekly, monthly, yearly, or fixed."
                    }
                }
            }
        },
        "specifications": {
            "type": "object",
            "description": "Additional listing specifications, for example wifi, kitchen, or workspace."
        },
        "guests": {
            "type": "integer",
            "minimum": 1
        },
        "beds": {
            "type": "integer",
            "minimum": 0
        },
        "bedrooms": {
            "type": "integer",
            "minimum": 0
        },
        "bathrooms": {
            "type": "integer",
            "minimum": 0
        },
        "active": {
            "type": "boolean"
        },
        "negotiable": {
            "type": "boolean"
        },
        "instantBook": {
            "type": "boolean"
        },
        "minStay": {
            "type": "integer",
            "minimum": 1
        },
        "checkIn": {
            "type": "string"
        },
        "checkOut": {
            "type": "string"
        },
        "quantity": {
            "type": "integer",
            "minimum": 1
        },
        "securityDeposit": {
            "type": "object",
            "additionalProperties": false,
            "required": [
                "value",
                "currency"
            ],
            "properties": {
                "value": {
                    "type": "string"
                },
                "currency": {
                    "type": "string"
                },
                "unit": {
                    "type": "string"
                },
                "decimals": {
                    "type": "integer",
                    "minimum": 0
                }
            }
        },
        "minPaymentAmount": {
            "type": "object",
            "additionalProperties": false,
            "required": [
                "value",
                "currency"
            ],
            "properties": {
                "value": {
                    "type": "string"
                },
                "currency": {
                    "type": "string"
                },
                "unit": {
                    "type": "string"
                },
                "decimals": {
                    "type": "integer",
                    "minimum": 0
                }
            }
        },
        "h3Tags": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "Optional precomputed H3 tags. If omitted, address is geocoded."
        },
        "h3FinestResolution": {
            "type": "integer",
            "minimum": 0
        },
        "h3MaxTags": {
            "type": "integer",
            "minimum": 1
        },
        "dryRun": {
            "type": "boolean",
            "default": true,
            "description": "True uploads local images to Blossom and previews the listing card only. Set false to publish after explicit approval."
        }
    }
},
    inputSchema: HostrListingsCreateInputSchema,
    readOnly: false,
  },
  {
    id: "hostr.listings.edit",
    toolName: "hostr_listings_edit",
    title: "Edit Hostr Listing",
    description: "Preview or publish a patch to an existing listing authored by the authenticated user. The live path also ensures seller configuration is published before signing the listing.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse when the authenticated author wants to update an existing listing. If the user has not named a concrete listing anchor, first call hostr_listings_list with mine=true and ask/choose from the returned listings. Patch only fields the user intends to change; preview with dryRun=true and publish with dryRun=false only after approval.",
    inputTypeName: "HostrListingsEditInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "anchor"
    ],
    "properties": {
        "anchor": {
            "type": "string",
            "description": "Listing naddr/a-tag anchor."
        },
        "patch": {
            "type": "object",
            "description": "Listing fields to change. Supports title, description, address, type, images, prices, specifications, guests, beds, bedrooms, bathrooms, active, negotiable, instantBook, quantity, securityDeposit, and minPaymentAmount."
        },
        "dryRun": {
            "type": "boolean",
            "default": true,
            "description": "True previews only. Set false to publish after explicit approval."
        }
    }
},
    inputSchema: HostrListingsEditInputSchema,
    readOnly: false,
  },
  {
    id: "hostr.listings.availability",
    toolName: "hostr_listings_availability",
    title: "Check Listing Availability",
    description: "Check whether one or more listings are available for a requested reservation date range. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nReservation date rule: Hostr reservation start/end inputs are calendar dates, not timezone instants. Preserve the dates the user requested and encode them as YYYY-MM-DDT00:00:00Z. The trailing Z is storage syntax only; do not convert from the user timezone, listing timezone, El Salvador time, check-in time, or check-out time.\n\nUse after a user has selected one or more listings and supplied dates, before booking or explaining date conflicts. Pass listing anchors from search/list results. If dates are missing, ask for them instead of guessing.",
    inputTypeName: "HostrListingsAvailabilityInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "start",
        "end"
    ],
    "properties": {
        "anchors": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "Listing anchors to check."
        },
        "anchor": {
            "type": "string",
            "description": "Single listing anchor alternative to anchors."
        },
        "start": {
            "type": "string",
            "format": "date-time",
            "description": "Requested start calendar date encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time."
        },
        "end": {
            "type": "string",
            "format": "date-time",
            "description": "Requested end calendar date encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time."
        }
    }
},
    inputSchema: HostrListingsAvailabilityInputSchema,
    readOnly: true,
  },
  {
    id: "hostr.listings.reviews",
    toolName: "hostr_listings_reviews",
    title: "Fetch Listing Reviews",
    description: "Fetch review events attached to one or more listings.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse when the user asks about reviews, reputation, prior guest feedback, or trust signals for one or more listings. Pass listing anchors from search/list results.",
    inputTypeName: "HostrListingsAnchorsInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "anchors": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "Listing anchors."
        },
        "anchor": {
            "type": "string",
            "description": "Single listing anchor."
        },
        "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 50
        }
    }
},
    inputSchema: HostrListingsAnchorsInputSchema,
    readOnly: true,
  },
  {
    id: "hostr.listings.reservationGroups",
    toolName: "hostr_listings_reservationGroups",
    title: "Fetch Listing Reservation Groups",
    description: "Fetch public reservation groups for one or more listings. Use this before availability-sensitive reservation workflows when the agent needs to explain conflicts.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse when the user asks why dates are unavailable, wants booking history/conflicts for a listing, or needs reservation context before changing availability-sensitive plans.",
    inputTypeName: "HostrListingsAnchorsInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "anchors": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "Listing anchors."
        },
        "anchor": {
            "type": "string",
            "description": "Single listing anchor."
        },
        "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 50
        }
    }
},
    inputSchema: HostrListingsAnchorsInputSchema,
    readOnly: true,
  },
  {
    id: "hostr.reservations.bookAndPay",
    toolName: "hostr_reservations_bookAndPay",
    title: "Book And Pay Reservation",
    description: "Use this foreground handoff tool whenever the user says to book, reserve, make, or create a reservation for an instant-book listing at or above the listed price. It creates the private reservation offer and escrow funding swap. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time. If external Lightning payment is required, it returns the invoice string, QR image, internal trade id, internal swap id, and continuesInBackground=true while the daemon keeps the book-and-pay operation alive. CRITICAL UI REQUIREMENT: leave only the QR image and invoice text visibly in the answer to the user; do not show internal trade id or swap id in the payment prompt, and do not replace the payment prompt with a summary. Only after that visible payment prompt is in the output, immediately call `hostr_swaps_watch` with the returned `swapId`, `tradeId`, `reservationWaitSeconds`, and `dryRun: false` to monitor payment/proof/reservation completion. When watch completes or cannot find the swap, query `hostr_trips_list` with `tradeId` until the committed reservation appears, then show a reservation card. Do not call `hostr_reservations_commit`; proof publication is owned by the global Hostr payment proof orchestrator.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nReservation date rule: Hostr reservation start/end inputs are calendar dates, not timezone instants. Preserve the dates the user requested and encode them as YYYY-MM-DDT00:00:00Z. The trailing Z is storage syntax only; do not convert from the user timezone, listing timezone, El Salvador time, check-in time, or check-out time.\n\nPrimary booking flow: use this when the user says book, reserve, make a reservation, create a reservation, or otherwise clearly wants an instant-book stay at or above the listed price. It creates the private offer, prepares escrow funding, returns external Lightning payment details when needed, and keeps the daemon-side book-and-pay operation alive. If invoice/QR are returned, show only the invoice string and QR image visibly in the payment prompt; keep internal tradeId and swapId hidden from the user-facing payment message. Immediately after the payment prompt is visible, call hostr_swaps_watch with swapId, tradeId, reservationWaitSeconds, and dryRun=false. When watch completes or cannot find the swap, call hostr_trips_list with the same tradeId until the committed reservation appears. Do not call hostr_reservations_commit for this normal path; proof publication is owned by the global payment proof orchestrator.",
    inputTypeName: "HostrReservationBookAndPayInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "listingAnchor",
        "start",
        "end"
    ],
    "properties": {
        "listingAnchor": {
            "type": "string",
            "description": "Listing naddr/a-tag anchor to instant-book."
        },
        "start": {
            "type": "string",
            "format": "date-time",
            "description": "Reservation start calendar date encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time."
        },
        "end": {
            "type": "string",
            "format": "date-time",
            "description": "Reservation end calendar date encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time."
        },
        "amount": {
            "type": "object",
            "additionalProperties": false,
            "required": [
                "value",
                "currency"
            ],
            "properties": {
                "value": {
                    "type": "string",
                    "description": "Decimal payment amount, represented as a string to avoid precision loss."
                },
                "currency": {
                    "type": "string",
                    "description": "Currency or denomination, such as USD or BTC."
                },
                "unit": {
                    "type": "string"
                },
                "decimals": {
                    "type": "integer",
                    "minimum": 0
                }
            },
            "description": "Optional reservation amount override. Must be at or above the listing price."
        },
        "escrowServiceId": {
            "type": "string",
            "description": "Optional escrow service id/pubkey/contract address. Omit to use the first compatible mutual escrow."
        },
        "proofTimeoutSeconds": {
            "type": "integer",
            "minimum": 30,
            "maximum": 3600,
            "default": 300,
            "description": "Seconds to wait for the global reservation stream to emit the committed reservation after swap completion."
        }
    }
},
    inputSchema: HostrReservationBookAndPayInputSchema,
    readOnly: false,
  },
  {
    id: "hostr.reservations.negotiateOffer",
    toolName: "hostr_reservations_negotiateOffer",
    title: "Create Reservation Negotiation Offer",
    description: "Create only a private negotiate-stage reservation offer. Use this for explicit negotiation/counteroffer requests, not for user intents like \"book\", \"reserve\", \"make a reservation\", or instant-book at the listed price; those must use hostr_reservations_bookAndPay instead.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nReservation date rule: Hostr reservation start/end inputs are calendar dates, not timezone instants. Preserve the dates the user requested and encode them as YYYY-MM-DDT00:00:00Z. The trailing Z is storage syntax only; do not convert from the user timezone, listing timezone, El Salvador time, check-in time, or check-out time.\n\nNegotiation-only flow: use for explicit offers, counteroffers, price/date negotiation, or non-instant-book reservation proposals. Do not use this for straightforward \"book/reserve\" intents on instant-book listings; use hostr_reservations_bookAndPay there. Preview with dryRun=true, then send the private negotiation event with dryRun=false only after approval.",
    inputTypeName: "HostrReservationsOfferInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "listingAnchor": {
            "type": "string",
            "description": "Listing naddr/a-tag anchor for a first offer."
        },
        "anchor": {
            "type": "string",
            "description": "Single listing anchor alternative to listingAnchor."
        },
        "tradeId": {
            "type": "string",
            "description": "Existing reservation trade id for a follow-up offer."
        },
        "start": {
            "type": "string",
            "format": "date-time",
            "description": "Reservation start calendar date for a first offer, encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time."
        },
        "end": {
            "type": "string",
            "format": "date-time",
            "description": "Reservation end calendar date for a first offer, encoded as YYYY-MM-DDT00:00:00Z. Reservation start/end values are calendar dates, not timezone instants. Encode the requested date as YYYY-MM-DDT00:00:00Z; the trailing Z is storage syntax only. Do not convert from the user timezone, listing timezone, check-in time, or check-out time."
        },
        "amount": {
            "type": "object",
            "additionalProperties": false,
            "required": [
                "value",
                "currency"
            ],
            "properties": {
                "value": {
                    "type": "string",
                    "description": "Decimal payment amount, represented as a string to avoid precision loss."
                },
                "currency": {
                    "type": "string",
                    "description": "Currency or denomination, such as USD or BTC."
                },
                "unit": {
                    "type": "string"
                },
                "decimals": {
                    "type": "integer",
                    "minimum": 0
                }
            },
            "description": "Optional reservation amount override. Omit to use listing price rules."
        },
        "dryRun": {
            "type": "boolean",
            "default": true,
            "description": "True only builds the offer and returns the event preview. Set false to send the gift-wrapped offer after explicit approval."
        },
        "timeoutSeconds": {
            "type": "integer",
            "minimum": 1,
            "maximum": 60,
            "default": 12
        }
    }
},
    inputSchema: HostrReservationsOfferInputSchema,
    readOnly: false,
  },
  {
    id: "hostr.reservations.negotiateAccept",
    toolName: "hostr_reservations_negotiateAccept",
    title: "Accept Reservation Negotiation",
    description: "Accept the latest private negotiate-stage reservation offer in a trade thread by replying with a matching negotiate-stage event.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse when the user wants to accept the latest private negotiated offer in a known trade thread. If tradeId is unknown, call hostr_updates, hostr_thread_view, hostr_trips_list, or hostr_bookings_list first to identify the trade.",
    inputTypeName: "HostrReservationTradeInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "tradeId"
    ],
    "properties": {
        "tradeId": {
            "type": "string"
        },
        "amount": {
            "type": "object",
            "additionalProperties": false,
            "required": [
                "value",
                "currency"
            ],
            "properties": {
                "value": {
                    "type": "string"
                },
                "currency": {
                    "type": "string"
                },
                "unit": {
                    "type": "string"
                },
                "decimals": {
                    "type": "integer",
                    "minimum": 0
                }
            }
        },
        "reason": {
            "type": "string"
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        },
        "timeoutSeconds": {
            "type": "integer",
            "minimum": 1,
            "maximum": 60,
            "default": 12
        }
    }
},
    inputSchema: HostrReservationTradeInputSchema,
    readOnly: false,
  },
  {
    id: "hostr.reservations.pay",
    toolName: "hostr_reservations_pay",
    title: "Pay Reservation Offer",
    description: "Preview or create the escrow funding swap for a payable reservation trade. The live action sends the escrow selection into the private thread as an unsigned child event, prepares the escrow fund calls, creates the Boltz swap invoice, and persists the payment context for commit.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nManual recovery/debug payment flow only. Normal AI-initiated instant-book payment should use hostr_reservations_bookAndPay. Use this when a negotiated or partially completed trade already exists and the user explicitly wants to create or inspect escrow funding for that trade.",
    inputTypeName: "HostrReservationPayInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "tradeId"
    ],
    "properties": {
        "tradeId": {
            "type": "string",
            "description": "Reservation trade id from negotiation updates."
        },
        "escrowServiceId": {
            "type": "string",
            "description": "Optional escrow service id/pubkey/contract address. Omit to use the first compatible mutual escrow."
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        },
        "timeoutSeconds": {
            "type": "integer",
            "minimum": 1,
            "maximum": 60,
            "default": 12
        }
    }
},
    inputSchema: HostrReservationPayInputSchema,
    readOnly: false,
  },
  {
    id: "hostr.reservations.commit",
    toolName: "hostr_reservations_commit",
    title: "Commit Paid Reservation",
    description: "Preview or publish the public commit-stage reservation after the escrow funding swap has completed and produced a claim transaction proof.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nManual recovery/debug commit flow only. Do not use after hostr_reservations_bookAndPay; that path relies on the global payment proof orchestrator. Use only when a swap proof already exists for a trade and the user explicitly needs to preview or publish the public commit-stage reservation.",
    inputTypeName: "HostrReservationCommitInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "swapId"
    ],
    "properties": {
        "swapId": {
            "type": "string",
            "description": "Boltz swap id returned by hostr_reservations_pay."
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        },
        "timeoutSeconds": {
            "type": "integer",
            "minimum": 1,
            "maximum": 60,
            "default": 12
        }
    }
},
    inputSchema: HostrReservationCommitInputSchema,
    readOnly: false,
  },
  {
    id: "hostr.reservations.cancel",
    toolName: "hostr_reservations_cancel",
    title: "Cancel Reservation",
    description: "Cancel either the private negotiate-stage reservation for a trade, or the committed public reservation if one exists.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse to cancel a private negotiation or committed reservation for a concrete trade. If tradeId is unclear, inspect updates, trips, bookings, or thread view first. Preview the cancellation and send with dryRun=false only after explicit approval.",
    inputTypeName: "HostrReservationTradeInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "tradeId"
    ],
    "properties": {
        "tradeId": {
            "type": "string"
        },
        "amount": {
            "type": "object",
            "additionalProperties": false,
            "required": [
                "value",
                "currency"
            ],
            "properties": {
                "value": {
                    "type": "string"
                },
                "currency": {
                    "type": "string"
                },
                "unit": {
                    "type": "string"
                },
                "decimals": {
                    "type": "integer",
                    "minimum": 0
                }
            }
        },
        "reason": {
            "type": "string"
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        },
        "timeoutSeconds": {
            "type": "integer",
            "minimum": 1,
            "maximum": 60,
            "default": 12
        }
    }
},
    inputSchema: HostrReservationTradeInputSchema,
    readOnly: false,
  },
  {
    id: "hostr.updates",
    toolName: "hostr_updates",
    title: "Fetch Hostr Updates",
    description: "Fetch the authenticated inbox, process gift-wrapped thread events, and summarize new offers and messages for the agent.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse as the inbox/home-state tool when the user asks for messages, offers, notifications, latest activity, what needs attention, or when you need trade/thread ids for negotiation or messaging. It processes gift-wrapped events and returns thread cards; present displayMarkdown, not raw event JSON.",
    inputTypeName: "HostrUpdatesInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 50
        },
        "timeoutSeconds": {
            "type": "integer",
            "minimum": 1,
            "maximum": 60,
            "default": 12
        }
    }
},
    inputSchema: HostrUpdatesInputSchema,
    readOnly: true,
  },
  {
    id: "hostr.reply",
    toolName: "hostr_reply",
    title: "Reply To Hostr Thread",
    description: "Preview or send a gift-wrapped text reply. Include conversation/tradeId to keep replies threaded.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nLegacy/general threaded reply tool. Prefer hostr_thread_message for user-facing conversation work because it returns a fixed thread-view contract. Use this when you already have the exact conversation/trade recipient context and need a simple gift-wrapped reply.",
    inputTypeName: "HostrReplyInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "content"
    ],
    "properties": {
        "content": {
            "type": "string"
        },
        "recipientPubkeys": {
            "type": "array",
            "items": {
                "type": "string"
            }
        },
        "recipientPubkey": {
            "type": "string"
        },
        "conversation": {
            "type": "string"
        },
        "tradeId": {
            "type": "string"
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        }
    }
},
    inputSchema: HostrReplyInputSchema,
    readOnly: false,
  },
  {
    id: "hostr.thread.view",
    toolName: "hostr_thread_view",
    title: "View Hostr Thread",
    description: "Load a Hostr conversation and return a fixed thread-view contract with message history. Use this when the user asks whether a host or escrow has messaged them, asks to see a conversation, or references a trip/booking thread.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse when the user asks to see a conversation, asks whether someone messaged them, references a trip/booking thread, or you need message history before replying. Prefer tradeId when the conversation is tied to a reservation; otherwise pass a known thread/conversation anchor from updates.",
    inputTypeName: "HostrThreadViewInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "threadAnchor": {
            "type": "string"
        },
        "anchor": {
            "type": "string"
        },
        "conversation": {
            "type": "string"
        },
        "tradeId": {
            "type": "string"
        },
        "recipientPubkeys": {
            "type": "array",
            "items": {
                "type": "string"
            }
        },
        "recipientPubkey": {
            "type": "string"
        },
        "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200
        },
        "timeoutSeconds": {
            "type": "integer",
            "minimum": 1,
            "maximum": 60,
            "default": 12
        }
    }
},
    inputSchema: HostrThreadViewInputSchema,
    readOnly: true,
  },
  {
    id: "hostr.thread.message",
    toolName: "hostr_thread_message",
    title: "Message Hostr Thread",
    description: "Preview or send a text message in a Hostr conversation, then return the fixed thread-view contract. Use this when the user asks to message a host, guest, buyer, seller, or an existing thread. If the user references a trade/trip, pass tradeId; use recipientRole when they say host, guest, buyer, seller, or escrow. Escrow messaging always requires a concrete tradeId and must include the trade buyer, seller, and escrow participants in one thread.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse when the user asks to message a host, guest, buyer, seller, escrow, or existing Hostr thread. If they reference a trip/booking/trade, pass tradeId. Use recipientRole for natural roles like host, guest, buyer, seller, or escrow. Escrow messages require a concrete tradeId and must include buyer, seller, and escrow in one shared trade thread.",
    inputTypeName: "HostrThreadMessageInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "content"
    ],
    "properties": {
        "content": {
            "type": "string"
        },
        "threadAnchor": {
            "type": "string"
        },
        "anchor": {
            "type": "string"
        },
        "conversation": {
            "type": "string"
        },
        "tradeId": {
            "type": "string"
        },
        "recipientRole": {
            "type": "string",
            "enum": [
                "host",
                "seller",
                "guest",
                "buyer",
                "escrow"
            ]
        },
        "role": {
            "type": "string"
        },
        "recipientPubkeys": {
            "type": "array",
            "items": {
                "type": "string"
            }
        },
        "recipientPubkey": {
            "type": "string"
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        },
        "timeoutSeconds": {
            "type": "integer",
            "minimum": 1,
            "maximum": 60,
            "default": 12
        }
    }
},
    inputSchema: HostrThreadMessageInputSchema,
    readOnly: false,
  },
  {
    id: "hostr.escrow.involve",
    toolName: "hostr_escrow_involve",
    title: "Involve Hostr Escrow",
    description: "Open or message the escrow conversation for a specific reservation trade, then return the fixed thread-view contract. This action always requires tradeId and always resolves the trade buyer, seller, and escrow participants into one shared trade thread; it must not create an escrow-only side conversation. If content is omitted, show the escrow trade thread and ask the user what to message the escrow. If content is provided, preview by default and send only with dryRun false.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse when the user explicitly asks to involve/message escrow for a specific reservation trade. Always pass tradeId. This opens the shared buyer/seller/escrow trade thread; never create an escrow-only side conversation. If no message content is provided, show the thread and ask what to send.",
    inputTypeName: "HostrEscrowInvolveInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "tradeId"
    ],
    "properties": {
        "tradeId": {
            "type": "string",
            "description": "Required reservation trade id. Escrow messages cannot be sent without a trade id because the thread must include buyer, seller, and escrow."
        },
        "content": {
            "type": "string"
        },
        "message": {
            "type": "string"
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        },
        "timeoutSeconds": {
            "type": "integer",
            "minimum": 1,
            "maximum": 60,
            "default": 12
        }
    }
},
    inputSchema: HostrEscrowInvolveInputSchema,
    readOnly: false,
  },
  {
    id: "hostr.profile.show",
    toolName: "hostr_profile_show",
    title: "Show Hostr Profile",
    description: "Show profile metadata for the authenticated MCP token pubkey session.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse when the user asks who they are on Hostr, wants their current profile, or before profile/listing publishing when you need existing metadata. This reads the profile for the MCP token pubkey.",
    inputTypeName: "HostrEmptyInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
},
    inputSchema: HostrEmptyInputSchema,
    readOnly: true,
  },
  {
    id: "hostr.profile.edit",
    toolName: "hostr_profile_edit",
    title: "Edit Hostr Profile",
    description: "Preview or publish the authenticated user profile metadata. Publishing also refreshes Hostr seller configuration.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse when the user wants to update profile name, about/bio, picture, banner, website, lightning address, or other profile metadata. Preview first; publish only after approval. Publishing also refreshes Hostr seller configuration, which is useful before creating or editing listings.",
    inputTypeName: "HostrProfileEditInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "name": {
            "type": "string"
        },
        "about": {
            "type": "string"
        },
        "image": {
            "type": "string"
        },
        "picture": {
            "type": "string"
        },
        "lud16": {
            "type": "string"
        },
        "nip05": {
            "type": "string"
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        }
    }
},
    inputSchema: HostrProfileEditInputSchema,
    readOnly: false,
  },
  {
    id: "hostr.trips.list",
    toolName: "hostr_trips_list",
    title: "List Hostr Trips",
    description: "List reservation groups involving the authenticated user as guest from the live userSubscriptions.myResolvedTripsList replay. Do not perform fresh Nostr reservation-by-author queries for this view. Return the fixed reservation-card display contract with resolved participant profile names. Pass `tradeId` after a book-and-pay swap watch completes or cannot find the swap to wait briefly for the committed public reservation and return it for display.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse for guest-side reservations: \"my trips\", \"my bookings as guest\", \"did my reservation complete\", or after book-and-pay/swap watch with tradeId to wait for the committed reservation card. Do not perform fresh reservation-by-author Nostr queries for this view.",
    inputTypeName: "HostrReservationCollectionInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 50
        },
        "tradeId": {
            "type": "string",
            "description": "Optional reservation trade id to look up directly after payment/proof completion."
        },
        "waitSeconds": {
            "type": "integer",
            "minimum": 0,
            "maximum": 300,
            "default": 15,
            "description": "How long to poll for a committed public reservation when tradeId is provided."
        }
    }
},
    inputSchema: HostrReservationCollectionInputSchema,
    readOnly: true,
  },
  {
    id: "hostr.bookings.list",
    toolName: "hostr_bookings_list",
    title: "List Hostr Bookings",
    description: "List reservation groups where the authenticated user is the host from the live userSubscriptions.myResolvedHostingsList replay. Do not perform fresh Nostr listing/reservation-by-author queries for this view. Return the fixed reservation-card display contract with resolved participant profile names.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse for host-side reservations on listings authored by the authenticated user: \"my bookings\", \"who booked my place\", \"hosting reservations\", or host calendar context. Do not perform fresh reservation-by-author Nostr queries for this view.",
    inputTypeName: "HostrReservationCollectionInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 50
        },
        "tradeId": {
            "type": "string",
            "description": "Optional reservation trade id to look up directly after payment/proof completion."
        },
        "waitSeconds": {
            "type": "integer",
            "minimum": 0,
            "maximum": 300,
            "default": 15,
            "description": "How long to poll for a committed public reservation when tradeId is provided."
        }
    }
},
    inputSchema: HostrReservationCollectionInputSchema,
    readOnly: true,
  },
  {
    id: "hostr.escrow.methods",
    toolName: "hostr_escrow_methods",
    title: "Show Hostr Escrow Methods",
    description: "Show mutual escrow methods and compatible services for a seller. If buyer is omitted, the authenticated token pubkey is used.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse before payment or when explaining how money is protected. It shows mutually compatible escrow methods/services between buyer and seller. If buyer is omitted, the authenticated token pubkey is used. Explain that Hostr swaps payment over Lightning into smart-contract escrow; the escrow service can only settle by forwarding or reversing according to trade outcome, not freely take custody.",
    inputTypeName: "HostrEscrowMethodsInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "user"
    ],
    "properties": {
        "user": {
            "type": "string",
            "description": "Seller/host pubkey to inspect escrow compatibility for."
        },
        "buyer": {
            "type": "string",
            "description": "Buyer pubkey. Defaults to the authenticated token pubkey."
        }
    }
},
    inputSchema: HostrEscrowMethodsInputSchema,
    readOnly: true,
  },
  {
    id: "hostr.escrow.service.list",
    toolName: "hostr_escrow_service_list",
    title: "List Escrow Services",
    description: "Escrow-only tool. List public escrow service events published by the authenticated escrow pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nEscrow-operator inventory view: list public escrow service events published by the authenticated escrow pubkey. Use before editing/deleting when the user has not selected a specific service event.",
    inputTypeName: "HostrEscrowServiceListInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25,
            "description": "Maximum number of escrow services to return."
        }
    }
},
    inputSchema: HostrEscrowServiceListInputSchema,
    readOnly: true,
    requiredRole: "escrow",
  },
  {
    id: "hostr.escrow.service.get",
    toolName: "hostr_escrow_service_get",
    title: "Get Escrow Service",
    description: "Escrow-only tool. Show one public escrow service event owned by the authenticated escrow pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nEscrow-operator detail view: inspect one escrow service event before explaining or editing settings. Use serviceId from hostr_escrow_service_list or user input.",
    inputTypeName: "HostrEscrowServiceGetInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "serviceId"
    ],
    "properties": {
        "serviceId": {
            "type": "string",
            "description": "Escrow service event id to inspect."
        }
    }
},
    inputSchema: HostrEscrowServiceGetInputSchema,
    readOnly: true,
    requiredRole: "escrow",
  },
  {
    id: "hostr.escrow.service.update",
    toolName: "hostr_escrow_service_update",
    title: "Update Escrow Service Settings",
    description: "Escrow-only tool. Preview or publish the authenticated escrow service parameters: fee percent, maximum trade duration, and per-token fee hints. Hidden unless the MCP token pubkey is in the daemon escrow pubkey allowlist. Use hostr.profile.edit for the escrow user profile; this tool only changes the public escrow service event.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nEscrow-operator settings workflow: preview changes to fee percent, maximum duration, or token fee hints. Keep dryRun=true until the user approves the exact preview. Use hostr_profile_edit for public profile/identity metadata; this tool only changes escrow service parameters.",
    inputTypeName: "HostrEscrowServiceUpdateInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "serviceId": {
            "type": "string",
            "description": "Optional escrow service event id to edit. Omit to edit the daemon bootstrap service."
        },
        "feePercent": {
            "type": "number",
            "minimum": 0,
            "maximum": 100,
            "description": "Proportional escrow fee as a percent, e.g. 1.5 for 1.5%."
        },
        "maxDurationSeconds": {
            "type": "integer",
            "minimum": 1,
            "maximum": 315360000,
            "description": "Maximum supported escrow duration in seconds. Omit to preserve the current value."
        },
        "tokenFeeHints": {
            "type": "object",
            "description": "Optional full replacement map keyed by token address, or \"native\". Values are smallest-unit fee hints.",
            "additionalProperties": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                    "baseFee": {
                        "type": "integer",
                        "minimum": 0
                    },
                    "maxFee": {
                        "type": "integer",
                        "minimum": 0
                    },
                    "minFee": {
                        "type": "integer",
                        "minimum": 0
                    }
                }
            }
        },
        "clearTokenFeeHints": {
            "type": "boolean",
            "default": false,
            "description": "Clear all per-token fee hints. Ignored if tokenFeeHints is provided."
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        }
    }
},
    inputSchema: HostrEscrowServiceUpdateInputSchema,
    readOnly: false,
    requiredRole: "escrow",
  },
  {
    id: "hostr.escrow.service.edit",
    toolName: "hostr_escrow_service_edit",
    title: "Edit Escrow Service Settings",
    description: "Escrow-only tool. Alias of service update using the preferred user-facing wording. Preview or publish the authenticated escrow service parameters. Use hostr.profile.edit for the escrow user profile.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nEscrow-operator settings workflow: preview changes to fee percent, maximum duration, or token fee hints. Keep dryRun=true until the user approves the exact preview. Use hostr_profile_edit for public profile/identity metadata; this tool only changes escrow service parameters.",
    inputTypeName: "HostrEscrowServiceUpdateInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "serviceId": {
            "type": "string",
            "description": "Optional escrow service event id to edit. Omit to edit the daemon bootstrap service."
        },
        "feePercent": {
            "type": "number",
            "minimum": 0,
            "maximum": 100,
            "description": "Proportional escrow fee as a percent, e.g. 1.5 for 1.5%."
        },
        "maxDurationSeconds": {
            "type": "integer",
            "minimum": 1,
            "maximum": 315360000,
            "description": "Maximum supported escrow duration in seconds. Omit to preserve the current value."
        },
        "tokenFeeHints": {
            "type": "object",
            "description": "Optional full replacement map keyed by token address, or \"native\". Values are smallest-unit fee hints.",
            "additionalProperties": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                    "baseFee": {
                        "type": "integer",
                        "minimum": 0
                    },
                    "maxFee": {
                        "type": "integer",
                        "minimum": 0
                    },
                    "minFee": {
                        "type": "integer",
                        "minimum": 0
                    }
                }
            }
        },
        "clearTokenFeeHints": {
            "type": "boolean",
            "default": false,
            "description": "Clear all per-token fee hints. Ignored if tokenFeeHints is provided."
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        }
    }
},
    inputSchema: HostrEscrowServiceUpdateInputSchema,
    readOnly: false,
    requiredRole: "escrow",
  },
  {
    id: "hostr.escrow.service.delete",
    toolName: "hostr_escrow_service_delete",
    title: "Delete Escrow Service",
    description: "Escrow-only destructive tool. Preview or publish a NIP-09 deletion for an escrow service event owned by the authenticated escrow pubkey. Keep dryRun true until the user explicitly approves deletion.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nEscrow-operator destructive workflow: preview deletion of a public escrow service event and require explicit deletion approval before dryRun=false. Include the reason when the user gives one.",
    inputTypeName: "HostrEscrowServiceDeleteInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "serviceId"
    ],
    "properties": {
        "serviceId": {
            "type": "string",
            "description": "Escrow service event id to delete."
        },
        "reason": {
            "type": "string",
            "description": "Optional deletion reason for the NIP-09 event."
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        }
    }
},
    inputSchema: HostrEscrowServiceDeleteInputSchema,
    readOnly: false,
    requiredRole: "escrow",
  },
  {
    id: "hostr.escrow.trades.list",
    toolName: "hostr_escrow_trades_list",
    title: "List Escrow Trades",
    description: "Escrow-only tool. List on-chain Hostr trades where the authenticated pubkey is a configured escrow. Hidden unless the MCP token pubkey is in the daemon escrow pubkey allowlist.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nEscrow-operator dashboard: list on-chain trades assigned to the authenticated escrow pubkey. Use before viewing/auditing/arbitrating when the user has not named a concrete tradeId.",
    inputTypeName: "HostrEscrowTradesListInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25,
            "description": "Maximum number of escrow trades to return."
        }
    }
},
    inputSchema: HostrEscrowTradesListInputSchema,
    readOnly: true,
    requiredRole: "escrow",
  },
  {
    id: "hostr.escrow.trades.view",
    toolName: "hostr_escrow_trades_view",
    title: "View Escrow Trade",
    description: "Escrow-only tool. View the on-chain state, event history, and Hostr reservation context for a trade assigned to the authenticated escrow pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nEscrow-operator trade detail: inspect on-chain state, event history, participants, amounts, and reservation context for a trade before audit or arbitration.",
    inputTypeName: "HostrEscrowTradeViewInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "tradeId"
    ],
    "properties": {
        "tradeId": {
            "type": "string",
            "description": "Hostr reservation trade id to inspect."
        }
    }
},
    inputSchema: HostrEscrowTradeViewInputSchema,
    readOnly: true,
    requiredRole: "escrow",
  },
  {
    id: "hostr.escrow.trades.audit",
    toolName: "hostr_escrow_trades_audit",
    title: "Audit Escrow Trade",
    description: "Escrow-only tool. Run a structured reservation and transition audit for a Hostr trade assigned to the authenticated escrow pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nEscrow-operator analysis: run a structured audit of reservation state and transitions for a trade before deciding whether arbitration is needed. This does not settle funds.",
    inputTypeName: "HostrEscrowTradeAuditInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "tradeId"
    ],
    "properties": {
        "tradeId": {
            "type": "string",
            "description": "Hostr reservation trade id to audit."
        }
    }
},
    inputSchema: HostrEscrowTradeAuditInputSchema,
    readOnly: true,
    requiredRole: "escrow",
  },
  {
    id: "hostr.escrow.trades.arbitrate",
    toolName: "hostr_escrow_trades_arbitrate",
    title: "Arbitrate Escrow Trade",
    description: "Escrow-only settlement tool. Preview or execute arbitration for a Hostr escrow trade. paymentForward and bondForward are fractions from 0 to 1. Keep dryRun true until the user explicitly approves the arbitration preview.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nEscrow-operator settlement workflow: preview paymentForward and bondForward fractions before execution. Fractions are 0 to 1. Explain the proposed split and reason to the user, then execute with dryRun=false only after explicit approval.",
    inputTypeName: "HostrEscrowArbitrateInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "tradeId",
        "paymentForward",
        "bondForward"
    ],
    "properties": {
        "tradeId": {
            "type": "string",
            "description": "Hostr reservation trade id to arbitrate."
        },
        "paymentForward": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "description": "Fraction of the escrowed payment to forward to the seller, from 0 to 1."
        },
        "bondForward": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "description": "Fraction of the escrow bond to forward according to the contract settlement rules, from 0 to 1."
        },
        "reason": {
            "type": "string",
            "description": "Optional human-readable arbitration reason."
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        }
    }
},
    inputSchema: HostrEscrowArbitrateInputSchema,
    readOnly: false,
    requiredRole: "escrow",
  },
  {
    id: "hostr.escrow.badges.definitions.list",
    toolName: "hostr_escrow_badges_definitions_list",
    title: "List Escrow Badge Definitions",
    description: "Escrow-only tool. List NIP-58 badge definitions published by the authenticated escrow pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nEscrow-operator badge inventory: list NIP-58 badge definitions issued by the authenticated escrow. Use before editing, awarding, or deleting badges when the identifier/anchor is unclear.",
    inputTypeName: "HostrEscrowBadgeDefinitionsListInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 50
        }
    }
},
    inputSchema: HostrEscrowBadgeDefinitionsListInputSchema,
    readOnly: true,
    requiredRole: "escrow",
  },
  {
    id: "hostr.escrow.badges.definitions.edit",
    toolName: "hostr_escrow_badges_definitions_edit",
    title: "Edit Escrow Badge Definition",
    description: "Escrow-only tool. Preview or publish a NIP-58 badge definition for the authenticated escrow pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nEscrow-operator badge definition workflow: preview or publish a NIP-58 badge definition. Use for creating or updating the badge name, description, or image. Publish only after approval.",
    inputTypeName: "HostrEscrowBadgeDefinitionEditInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "identifier",
        "name"
    ],
    "properties": {
        "identifier": {
            "type": "string",
            "description": "Badge definition d-tag identifier."
        },
        "name": {
            "type": "string"
        },
        "description": {
            "type": "string"
        },
        "image": {
            "type": "string"
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        }
    }
},
    inputSchema: HostrEscrowBadgeDefinitionEditInputSchema,
    readOnly: false,
    requiredRole: "escrow",
  },
  {
    id: "hostr.escrow.badges.definitions.delete",
    toolName: "hostr_escrow_badges_definitions_delete",
    title: "Delete Escrow Badge Definition",
    description: "Escrow-only destructive tool. Preview or publish a NIP-09 deletion for a badge definition owned by the authenticated escrow pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nEscrow-operator destructive badge workflow: preview deletion of a badge definition and publish only after explicit approval. Include a reason when provided.",
    inputTypeName: "HostrEscrowBadgeDefinitionDeleteInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "anchor"
    ],
    "properties": {
        "anchor": {
            "type": "string"
        },
        "reason": {
            "type": "string"
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        }
    }
},
    inputSchema: HostrEscrowBadgeDefinitionDeleteInputSchema,
    readOnly: false,
    requiredRole: "escrow",
  },
  {
    id: "hostr.escrow.badges.awards.list",
    toolName: "hostr_escrow_badges_awards_list",
    title: "List Escrow Badge Awards",
    description: "Escrow-only tool. List NIP-58 badge awards issued by the authenticated escrow pubkey, optionally filtered by definition anchor.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nEscrow-operator badge award view: list issued badge awards, optionally filtered by definition anchor. Use before revoking when the award id is unclear.",
    inputTypeName: "HostrEscrowBadgeAwardsListInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "definitionAnchor": {
            "type": "string"
        },
        "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 50
        }
    }
},
    inputSchema: HostrEscrowBadgeAwardsListInputSchema,
    readOnly: true,
    requiredRole: "escrow",
  },
  {
    id: "hostr.escrow.badges.award",
    toolName: "hostr_escrow_badges_award",
    title: "Award Escrow Badge",
    description: "Escrow-only tool. Preview or publish a NIP-58 badge award from the authenticated escrow pubkey to a recipient pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nEscrow-operator award workflow: preview or publish a NIP-58 badge award to a recipient pubkey, optionally tied to a listing anchor. Publish only after approval.",
    inputTypeName: "HostrEscrowBadgeAwardInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "definitionAnchor",
        "recipientPubkey"
    ],
    "properties": {
        "definitionAnchor": {
            "type": "string"
        },
        "recipientPubkey": {
            "type": "string"
        },
        "listingAnchor": {
            "type": "string"
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        }
    }
},
    inputSchema: HostrEscrowBadgeAwardInputSchema,
    readOnly: false,
    requiredRole: "escrow",
  },
  {
    id: "hostr.escrow.badges.revoke",
    toolName: "hostr_escrow_badges_revoke",
    title: "Revoke Escrow Badge Award",
    description: "Escrow-only destructive tool. Preview or publish a NIP-09 deletion for a badge award issued by the authenticated escrow pubkey.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nEscrow role notes: this tool is visible only when the authenticated Hostr pubkey is configured as an escrow service. It is for escrow-operator work, not ordinary guest/host booking flows. Keep user profile edits in hostr_profile_edit; escrow service tools only manage escrow service events/settings.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nEscrow-operator destructive award workflow: preview revocation/deletion of an issued badge award and publish only after explicit approval. Include a reason when provided.",
    inputTypeName: "HostrEscrowBadgeRevokeInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "awardId"
    ],
    "properties": {
        "awardId": {
            "type": "string"
        },
        "reason": {
            "type": "string"
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        }
    }
},
    inputSchema: HostrEscrowBadgeRevokeInputSchema,
    readOnly: false,
    requiredRole: "escrow",
  },
  {
    id: "hostr.swaps.watch",
    toolName: "hostr_swaps_watch",
    title: "Watch Hostr Swap",
    description: "Inspect a persisted swap-in by id and, when dryRun is false, run safe recovery until proof or terminal state. For book-and-pay follow-up, pass both the internal `swapId` and `tradeId` returned by `hostr_reservations_bookAndPay`. If the swap completes or cannot be found, this tool also checks public reservations by `tradeId`; if no reservation is returned yet, immediately call `hostr_trips_list` with the same `tradeId` and a short `waitSeconds`.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse immediately after hostr_reservations_bookAndPay returns swapId/tradeId, or for manual swap recovery. With dryRun=false it safely resumes/polls the persisted swap until proof, terminal failure, or timeout. After completion or swap-not-found fallback, call hostr_trips_list with tradeId to surface the committed reservation card.",
    inputTypeName: "HostrSwapsWatchInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "required": [
        "swapId"
    ],
    "properties": {
        "swapId": {
            "type": "string"
        },
        "tradeId": {
            "type": "string",
            "description": "Optional reservation trade id used to fall back to public reservation lookup after the swap completes or is not found."
        },
        "reservationWaitSeconds": {
            "type": "integer",
            "minimum": 0,
            "maximum": 300,
            "default": 20,
            "description": "How long to poll for the committed reservation after proof completion or swap-not-found fallback."
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        }
    }
},
    inputSchema: HostrSwapsWatchInputSchema,
    readOnly: false,
  },
  {
    id: "hostr.swaps.recoverAll",
    toolName: "hostr_swaps_recoverAll",
    title: "Recover Hostr Swaps",
    description: "Preview or run recovery for all persisted swap-in and swap-out operations.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nWrite behavior: this tool can create, publish, send, recover, delete, pay, arbitrate, or otherwise change state outside ChatGPT. Explain the important effect to the user before live execution, preserve user-visible previews, and require explicit user approval before any irreversible or externally visible action.\n\nPreview rule: dryRun defaults to true. First call with dryRun=true, show the user the returned preview or card, and only repeat with dryRun=false after the user explicitly approves that preview in the conversation. Do not treat vague acknowledgement as approval for destructive, payment, publication, messaging, cancellation, recovery, or arbitration actions.\n\nUse when the user asks to recover stuck payments/swaps or when diagnostics show persisted swap operations need resumption. Preview first; run with dryRun=false only after approval. Use background=true only when the user wants recovery to continue asynchronously.",
    inputTypeName: "HostrSwapsRecoverAllInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "background": {
            "type": "boolean",
            "default": false
        },
        "dryRun": {
            "type": "boolean",
            "default": true
        }
    }
},
    inputSchema: HostrSwapsRecoverAllInputSchema,
    readOnly: false,
  },
  {
    id: "hostr.swaps.list",
    toolName: "hostr_swaps_list",
    title: "List Hostr Swaps",
    description: "List persisted swap-in and swap-out operation states.\n\nMCP driving notes: Hostr is the canonical tool surface for Hostr marketplace state and Hostr-related Nostr state, including listings, reservations, trips, bookings, inbox threads, Nostr Connect/NIP-46 signer login, relays, npubs/naddrs, gift-wrapped messages, escrow services, swaps, and on-chain escrow trades. Do not use general web search for these live Hostr/Nostr workflows unless the user explicitly asks for public web documentation. The MCP access token selects the authenticated Hostr pubkey/session; do not invent or pass a user pubkey unless this tool has a parameter that explicitly asks for an author, buyer, seller, recipient, or escrow pubkey. If an authenticated write fails because the signer is missing or offline, call hostr_session_connect and then retry the original workflow.\n\nRead-only behavior: this tool retrieves or analyzes Hostr state and is safe to call when the user asks to inspect, search, explain, debug, or choose the next action. Prefer read tools before write tools when the user intent is ambiguous or when you need concrete listing, trade, thread, or profile ids.\n\nUse to inspect persisted swap-in and swap-out states before recovery/debugging, or when the user asks about payment status, stuck swaps, refunds, or pending Lightning/on-chain operations.",
    inputTypeName: "HostrSwapsListInput",
    inputJsonSchema: {
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "namespace": {
            "type": "string",
            "enum": [
                "all",
                "swap_in",
                "swap_out"
            ],
            "default": "all"
        }
    }
},
    inputSchema: HostrSwapsListInputSchema,
    readOnly: true,
  },
] as const;

export type HostrActionId = (typeof hostrActionCatalog)[number]["id"];

export const hostrActionByToolName = new Map(hostrActionCatalog.map((action) => [action.toolName, action]));

export const hostrActionById = new Map(hostrActionCatalog.map((action) => [action.id, action]));
